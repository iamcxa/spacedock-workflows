# Fixture spec — No trigger path

## Problem

A bash script needs refactoring for better error handling. No UI changes, no schema domain.

## Acceptance Outcome

Script exits with code 1 on failure with descriptive error messages.
