# Fixture spec — M1 HALT path (schema domain, missing specialist)

## Problem

New drizzle schema migration for the payments table. L1 decider fmodel update required.

## Acceptance Outcome

Migration runs without errors; fmodel column validated via drizzle schema check.
