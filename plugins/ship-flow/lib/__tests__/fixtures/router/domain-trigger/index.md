---
id: "fixture-domain-trigger"
title: "Domain trigger fixture — affects_ui=false, domain=schema"
status: sharp
affects_ui: false
domain: schema
---

# Fixture: Domain trigger path

Entity with `affects_ui: false` and `domain: schema` set by shape via registry-classify.
Expected router behavior: design fires via specialist path (registry-resolve consulted).
