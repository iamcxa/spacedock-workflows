---
id: "fixture-ui-trigger"
title: "UI trigger fixture — affects_ui=true, no domain"
status: sharp
affects_ui: true
---

# Fixture: UI trigger path

Entity with `affects_ui: true` and no `domain:` set.
Expected router behavior: design fires via UI path (Category 0 / A/B/C/D classification).
