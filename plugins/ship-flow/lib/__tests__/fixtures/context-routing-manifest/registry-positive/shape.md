# Context routing registry fixture

This fixture touches schema routing and context routing manifest behavior.
It mentions drizzle schema changes and fmodel contracts so the registry
classifies the local `schema` domain without any external context provider.
