# Verify Prose Only Fixture

### Verify Check Manifest

The verifier says routed context was considered, but it has no extracted
`context-routing-manifest` input and no `manifest_required_skill` row.
