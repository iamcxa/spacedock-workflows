# Verify Empty Extraction Fixture

This plan only mentions context-routing-manifest in prose. It intentionally
omits the section tag so `extract-section.sh <plan.md> context-routing-manifest`
returns empty.
