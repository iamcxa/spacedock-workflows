# fixture: m1-specialist-missing/defaults.yaml
# Tests: M1 path — domain matched, designer_section_anchor references nonexistent anchor
# registry-resolve.sh --validate --domain=schema should exit 10, stderr status=specialist_missing
schema_version: "1.0"
domains:
  schema:
    trigger_patterns: []
    spec_keywords:
      - schema
      - drizzle
    knowledge_module: plugins/ship-flow/references/domain-knowledge/schema.md
    designer_section_anchor: ""
    description: "Schema design (M1 fixture — specialist anchor does not exist)"
