# Attempt-Scoped Stage Circuits Implementation Plan

> **For executer:** REQUIRED SUB-SKILL: use `superpowers:executing-plans` task-by-task and preserve the Ship-Flow TDD ledger.

**Goal:** Make plan/execute attempts typed, recoverable, replay-safe, and bounded without changing frozen completion-v1 bytes or #21's technical work.
**Architecture:** A plan/execute-only `fo-stage-attempt.sh` wraps, but never alters, completion-v1. A common-Git-dir WAL/return bundle owns nonterminal state; layout-resolved tracked sidecars own terminal evidence; skills consume only FO timing and route decisions.
**Tech stack:** Bash 3.2+, Git temporary-index/ref-CAS fixtures, Python 3 ledger validator, Node 18+ CI gates.

<details>
<summary>Mechanical design import</summary>

## Plan Imported Design DCs
<!-- section:plan-imported-design-dcs -->
<!-- Generated by plugins/ship-flow/lib/import-design-dcs.sh from entity's ### Hand-off to Plan block. Do not hand-edit; rerun the script if the source hand-off changes. -->
| # | Type | Assertion | Rationale (D{N}) | Source artifact |
|---|---|---|---|---|
| 1 | contract | Keep completion-v1 lease and receipt serialization byte-exact; implement plan/execute identity as a separate exact stage-attempt-v1 outer receipt, and on passed folder runs bind the separately delimited unchanged completion receipt by SHA-256. | D1 | docs/ship-flow/attempt-scoped-stage-circuits/design.md |
| 2 | contract | Limit attempt-v1 integration to plan and execute; shape, design, verify, review, ship, PR-feedback, task-review, and unrelated circuit breakers retain current behavior. | D1 | docs/ship-flow/attempt-scoped-stage-circuits/design.md |
| 3 | schema-contract | FO alone issues stage_run_id, attempt_id, ordinal, per-attempt before OID, audit start, per-stage budget, lease binding, and monotonic elapsed values; worker return must bind the same ref and before OID plus its completion OID, and no worker field can reset authority. | D2 | docs/ship-flow/attempt-scoped-stage-circuits/design.md |
| 4 | contract | Same-boot resume preserves the suspended attempt, ID, ordinal, start, monotonic origin, budget, and continuation count; lost clock identity terminalizes interrupted, while returned-receipt replay never dispatches or adds duration twice. | D2 | docs/ship-flow/attempt-scoped-stage-circuits/design.md |
| 5 | data-contract | Use a git-common-dir exclusion lock and WAL keyed by canonical entity plus stage only, with stage_run_id bound inside the WAL, so same-entity sibling runs contend even under different entry OIDs; retain exact returned bytes in a common-Git-dir sidecar, then atomically append canonical terminal history plus the exact tracked returned-bundle sidecar using temporary-index ref CAS, and place passed folder history after the unchanged completion checkpoint. | D3 | docs/ship-flow/attempt-scoped-stage-circuits/design.md |
| 6 | data-contract | Canonical receipt/history replay is idempotent and returns already-recorded; a second or byte-conflicting terminal fails closed without changing WAL, history, ref, index, or worktree bytes. | D3 | docs/ship-flow/attempt-scoped-stage-circuits/design.md |
| 7 | contract | Bind policy to the FO stage-entry commit OID, preserve plan budget 1200 seconds and execute budget 1800 seconds, and permit at most one fresh continuation after a typed or legacy partial/interrupted result. | D4 | docs/ship-flow/attempt-scoped-stage-circuits/design.md |
| 8 | contract | A second fresh-continuation request emits one idempotent route-out record with route=return and reason=attempt-count-exhausted before completion lease acquisition, attempt creation, envelope construction, or worker dispatch; blocked/failed route immediately. | D4 | docs/ship-flow/attempt-scoped-stage-circuits/design.md |
| 9 | data-contract | Preserve duration_minutes as compatibility data and add stage_run_id, attempt identity/timing, before/completion OIDs, returned-bundle digest, fresh_continuations_used, state/outcome, cumulative elapsed, and terminal event projections to plan and execute reports; cumulative duration never gates a fresh continuation. | D2 | docs/ship-flow/attempt-scoped-stage-circuits/design.md |
| 10 | contract | #21 dogfood hashes and preserves the exact 32-minute and 61-minute receipt blocks, five-record plan ledger, allocator plan, and product diff; legacy-unscoped contributes 5580 cumulative seconds and makes the one new typed revalidation consume the sole fresh-continuation allowance. | D4 | docs/ship-flow/attempt-scoped-stage-circuits/design.md |
No visible/render/whole-page rows; non-UI. `open_decisions: []`.
<!-- /section:plan-imported-design-dcs -->
</details>

## Plan Output

### Research Summary

Producer found one M-size helper plus five RED suites. Current completion lease/receipt bytes are centralized (`completion-v1.sh:66-73,217-239`), extra fields fail (`fo-completion-lifecycle.sh:22-30`), and the 2026-07-19 debrief requires injected crash/lease faults. Research review found unsafe test ordering; addressed by W0 writing/running every new RED suite before production edits. Reviewer assessment: GAPS_ADDRESSED; no contradiction.

### Size Re-evaluation

- Sharp/adjusted: M at its 15-path upper bound, 2.3/3 days; five serial tasks.
- Hard cut: if exact returned-sidecar/ref-CAS fault coverage cannot fit three days, return narrow; never drop crash tests or add scheduling.

### Assumption Re-validation

- A1-A3 content-match current source; `fo-completion-lease.sh` is worktree-local absolute-git-dir, so only the new common-Git-dir attempt lock excludes siblings.
- A4 is pinned at commit `d939cf9ba5794640a5830440f89bbb82d0b1f16e`: entity lines 607-635 SHA-256 `23aa88f981b8182a1600199bc4e572df508c4ecd00f1befc62f1d60070b57ffc`; lines 637-664 `c6cd94e5e8e60443286297193fdff62612a12b67b860b4e5768b82cc08afd00c`; five-line ledger `cf0ee9f001554c8d26216130a40fe6ecf7f39450022c68bf264cdef24cfadffb`. Execute re-proves all three before edits.
- Runtime: Tier-2 shell/Node; Bash tests, `bash -n`, shellcheck, Python ledger, Node 18+ CI. No build/dev command applies.

### Verification Spec

| DC | Verify Procedure | Expected |
|---|---|---|
| DC-1 (W1) | `bash plugins/ship-flow/lib/__tests__/test-stage-attempt-clock.sh && bash plugins/ship-flow/lib/__tests__/test-stage-wiring.sh --stage-attempts` | Fresh plan/execute continuation passes under 1200/1800 while cumulative elapsed stays monotonic. |
| DC-2 (W2) | `bash plugins/ship-flow/lib/__tests__/test-stage-attempt-history.sh` | Resume/replay and every named crash/lease/CAS fault yield one terminal contribution and preserve dirt. |
| DC-3 (W3) | `bash plugins/ship-flow/lib/__tests__/test-stage-attempt-route.sh` | Threshold+1 and blocked/failed return before every dispatch side effect; replay adds no route line. |
| DC-4 (W4) | `bash plugins/ship-flow/lib/__tests__/test-attempt-scoped-stage-circuits-21.sh` | Both report-block hashes and five-record ledger hash stay exact; seed=5580; exactly one typed continuation. |

### Canonical Doc Actions

| Doc | Action | Source | Rationale |
|---|---|---|---|
| ROADMAP.md | update | spec | Record approved active prerequisite; review owns atomic write. |
| PRODUCT.md | skip | design | Internal recovery correctness, no user-facing capability. |
| ARCHITECTURE.md | update | design | Record FO attempt authority, WAL/terminal ownership, replay, and bounded routing. |

<details>
<summary>Scope anchoring</summary>

### Scope Anchoring

| Task | Mapping |
|---|---|
| T0 | W1-W4 test contracts; imports 1-10 |
| T1 | ASC.1/W1; imports 1-4,7,9 |
| T2 | ASC.2/W2; imports 4-6 |
| T3 | ASC.2/W3/W4; imports 7-8,10 |
| T4 | W1-W4 integration; imports 1-10 |

</details>

### Plan

#### T0 — Freeze all attempt-circuit RED contracts
task_id: T0
layer: L5
wave: W0
files: create `plugins/ship-flow/lib/__tests__/test-stage-attempt-v1-contract.sh`, `plugins/ship-flow/lib/__tests__/test-stage-attempt-clock.sh`, `plugins/ship-flow/lib/__tests__/test-stage-attempt-history.sh`, `plugins/ship-flow/lib/__tests__/test-stage-attempt-route.sh`, `plugins/ship-flow/lib/__tests__/test-attempt-scoped-stage-circuits-21.sh`
read_first: `design.md:165-319,371-388`; current completion/history fixtures; live #21 ref
classification: all five files MISSING; prove via `test ! -e` and two-strategy `find` + `rg` absence.
skills_needed: [test]
reviewer_questions: [{lens: tdd, question: "Do frozen tests cover every exact byte, clock, returned-sidecar/checkpoint/CAS/replay order, route, and #21 invariant before production edits?", affected_path_family: "plugins/ship-flow/lib/__tests__/**", evidence_required: "all five scripts syntax-clean and fail for the named missing helper behavior"}]
TDD: skip -- test-only RED scaffold intentionally stays failing until T1-T3; `bash -n` is alternate validation.
parallel_group: serial; depends_on: []; owned_paths: [plugins/ship-flow/lib/__tests__/test-stage-attempt-v1-contract.sh, plugins/ship-flow/lib/__tests__/test-stage-attempt-clock.sh, plugins/ship-flow/lib/__tests__/test-stage-attempt-history.sh, plugins/ship-flow/lib/__tests__/test-stage-attempt-route.sh, plugins/ship-flow/lib/__tests__/test-attempt-scoped-stage-circuits-21.sh]; integration_owner: executer@attempt-scoped-stage-circuits
steps: 1. Write exact tests only. 2. `bash -n` all five. 3. Run all five and retain expected REDs before any helper/schema/skill edit. 4. Commit test paths explicitly.
done: all five fail on named absent behavior, never syntax/fixture setup; model: sonnet; canonical_doc_actions: shared.

#### T1 — Exact protocol, FO identity, and clock
task_id: T1
layer: L5
wave: W1
files: create `plugins/ship-flow/lib/fo-stage-attempt.sh`
read_first: T0 RED; `design.md:101-255`; `completion-v1.sh:66-73,217-239`; `fo-completion-lease.sh:20-49`
classification: helper MISSING; `rg 'stage-attempt-v1' plugins/ship-flow/lib` finds design/tests only.
skills_needed: [test, best-practices, test-driven-development]
reviewer_questions: [{lens: contract, question: "Do exact WAL/receipt/frame bytes preserve completion-v1 and reject non-plan/execute/foreign binding?", affected_path_family: "plugins/ship-flow/lib/**", evidence_required: "focused RED/GREEN and frozen completion fixture digests"}]
tdd_contract:
  red_command: "set +e; bash plugins/ship-flow/lib/__tests__/test-stage-attempt-v1-contract.sh; r1=$?; bash plugins/ship-flow/lib/__tests__/test-stage-attempt-clock.sh; r2=$?; test $r1 -ne 0 && test $r2 -ne 0"
  expected_red_failure: "Exact grammar, allowlist, FO identity, and fresh/resume clock assertions fail because the helper is absent."
  green_command: "bash plugins/ship-flow/lib/__tests__/test-stage-attempt-v1-contract.sh && bash plugins/ship-flow/lib/__tests__/test-stage-attempt-clock.sh && bash plugins/ship-flow/lib/__tests__/test-completion-v1-review.sh"
  refactor_check: "bash -n plugins/ship-flow/lib/fo-stage-attempt.sh && shellcheck plugins/ship-flow/lib/fo-stage-attempt.sh"
parallel_group: serial; depends_on: [T0]; owned_paths: [plugins/ship-flow/lib/fo-stage-attempt.sh]; integration_owner: executer@attempt-scoped-stage-circuits
steps: 1. Re-run focused RED. 2. Implement minimal exact parser/begin/suspend/resume and portable clocks. 3. GREEN/refactor; explicit-path commit.
done: focused GREEN and completion-v1 digest fixtures unchanged; model: sonnet; canonical_doc_actions: shared.

#### T2 — Crash-safe return, checkpoint-ordered history, and replay
task_id: T2
layer: L5
wave: W2
files: modify `plugins/ship-flow/lib/fo-stage-attempt.sh`
read_first: T1; `design.md:209-291`; `fo-reconcile-completion.sh:31-59`; `_debriefs/2026-07-19-01.md:98-105`
classification: helper recovery seam EXISTS_BROKEN until the frozen history test passes; disproof is crash after sidecar write/before WAL flip.
skills_needed: [test, best-practices, test-driven-development]
reviewer_questions: [{lens: recovery, question: "Is order exact: returned write -> WAL flip -> passed-folder completion checkpoint/reconcile/lease cleanup -> terminal ledger+tracked-sidecar CAS -> path-only reconcile -> cleanup/replay?", affected_path_family: "plugins/ship-flow/lib/**", evidence_required: "fault injection at every arrow; non-passed/flat bypass completion checkpoint and terminalize directly"}]
tdd_contract:
  red_command: "bash plugins/ship-flow/lib/__tests__/test-stage-attempt-history.sh"
  expected_red_failure: "Provisional adoption, sibling exclusion, exact replay, temporary-index CAS, and dirty-state faults remain RED after T1."
  green_command: "bash plugins/ship-flow/lib/__tests__/test-stage-attempt-history.sh"
  refactor_check: "bash -n plugins/ship-flow/lib/fo-stage-attempt.sh && shellcheck plugins/ship-flow/lib/fo-stage-attempt.sh"
parallel_group: serial; depends_on: [T1]; owned_paths: [plugins/ship-flow/lib/fo-stage-attempt.sh]; integration_owner: executer@attempt-scoped-stage-circuits
steps: 1. Re-run history RED. 2. Implement exact `.returned`, conditional checkpoint order, tracked sidecar/history CAS, replay/fail-closed cleanup. 3. GREEN/refactor; explicit-path commit.
done: every injected boundary yields one terminal contribution and unchanged unrelated bytes; model: sonnet; canonical_doc_actions: shared.

#### T3 — Bounded route-out and immutable #21 legacy seed
task_id: T3
layer: L5
wave: W3
files: modify `plugins/ship-flow/lib/fo-stage-attempt.sh`
read_first: T2; `design.md:294-319`; pinned #21 commit `d939cf9ba5794640a5830440f89bbb82d0b1f16e` entity/plan/ledger blobs and expected hashes in Assumption Re-validation
classification: route/legacy seams MISSING; generic scheduling confirmed excluded by `rg` in shape/design and helper tree.
skills_needed: [test, test-driven-development]
reviewer_questions: [{lens: policy, question: "Are 1200/1800, strict > expiry, one continuation, pre-side-effect return, and hash-only #21 seed exact?", affected_path_family: "plugins/ship-flow/lib/**", evidence_required: "side-effect counters plus pre/post hashes of two report blocks and five-record ledger"}]
tdd_contract:
  red_command: "set +e; bash plugins/ship-flow/lib/__tests__/test-stage-attempt-route.sh; r1=$?; bash plugins/ship-flow/lib/__tests__/test-attempt-scoped-stage-circuits-21.sh; r2=$?; test $r1 -ne 0 && test $r2 -ne 0"
  expected_red_failure: "No typed exhaustion/blocked/failed route or immutable legacy-unscoped seed exists after T2."
  green_command: "bash plugins/ship-flow/lib/__tests__/test-stage-attempt-route.sh && bash plugins/ship-flow/lib/__tests__/test-attempt-scoped-stage-circuits-21.sh"
  refactor_check: "bash -n plugins/ship-flow/lib/fo-stage-attempt.sh && shellcheck plugins/ship-flow/lib/fo-stage-attempt.sh"
parallel_group: serial; depends_on: [T2]; owned_paths: [plugins/ship-flow/lib/fo-stage-attempt.sh]; integration_owner: executer@attempt-scoped-stage-circuits
steps: 1. Re-run both REDs. 2. Implement one-continuation routes and one-time hash-only seed=5580. 3. GREEN, re-hash live evidence, explicit-path commit.
done: threshold+1 has zero dispatch side effects; live #21 bytes/ledger remain exact; model: sonnet; canonical_doc_actions: shared.

#### T4 — Plan/execute wiring, report schema, and full regression
task_id: T4
layer: L1
wave: W4
files: modify `plugins/ship-flow/lib/fo-completion-lifecycle.sh`, `plugins/ship-flow/skills/ship-plan/SKILL.md`, `plugins/ship-flow/skills/ship-execute/SKILL.md`, `plugins/ship-flow/references/entity-body-schema.yaml`, `plugins/ship-flow/lib/__tests__/test-stage-wiring.sh`, `plugins/ship-flow/lib/__tests__/test-entity-body-schema.sh`, `plugins/ship-flow/lib/__tests__/test-completion-v1-review.sh`, `plugins/ship-flow/lib/__tests__/test-completion-v1-frontmatter.sh`, `plugins/ship-flow/lib/__tests__/test-advance-stage.sh`
read_first: T1-T3; design Contract/Test Surface; every listed current test
classification: completion seam WORKING; schema/skills EXISTS_BROKEN for prose-only cumulative breakers; disproof is new `--stage-attempts` wiring mode.
skills_needed: [test, test-driven-development, write-docs]
reviewer_questions: [{lens: integration, question: "Do only plan/execute consume FO timing while completion-v1 bytes and unrelated stages/breakers remain unchanged?", affected_path_family: "plugins/ship-flow/{lib,skills,references}/**", evidence_required: "focused matrices plus full invariants/shell/node/version/no-dangling gates"}]
tdd_contract:
  red_command: "set +e; bash plugins/ship-flow/lib/__tests__/test-stage-wiring.sh --stage-attempts; r1=$?; bash plugins/ship-flow/lib/__tests__/test-entity-body-schema.sh; r2=$?; test $r1 -ne 0 && test $r2 -ne 0"
  expected_red_failure: "New plan/execute wiring and typed projection assertions fail before lifecycle/skill/schema edits; unrelated stages are negative controls."
  green_command: "bash plugins/ship-flow/lib/__tests__/test-stage-wiring.sh --stage-attempts && bash plugins/ship-flow/lib/__tests__/test-entity-body-schema.sh && bash plugins/ship-flow/lib/__tests__/test-completion-v1-review.sh && bash plugins/ship-flow/lib/__tests__/test-completion-v1-frontmatter.sh && bash plugins/ship-flow/lib/__tests__/test-advance-stage.sh"
  refactor_check: "bash -n plugins/ship-flow/lib/fo-completion-lifecycle.sh plugins/ship-flow/lib/__tests__/test-stage-wiring.sh && git diff --check"
parallel_group: serial; depends_on: [T3]; owned_paths: [plugins/ship-flow/lib/fo-completion-lifecycle.sh, plugins/ship-flow/skills/ship-plan/SKILL.md, plugins/ship-flow/skills/ship-execute/SKILL.md, plugins/ship-flow/references/entity-body-schema.yaml, plugins/ship-flow/lib/__tests__/test-stage-wiring.sh, plugins/ship-flow/lib/__tests__/test-entity-body-schema.sh, plugins/ship-flow/lib/__tests__/test-completion-v1-review.sh, plugins/ship-flow/lib/__tests__/test-completion-v1-frontmatter.sh, plugins/ship-flow/lib/__tests__/test-advance-stage.sh]; integration_owner: executer@attempt-scoped-stage-circuits
steps: 1. Add only integration/schema assertions; run RED. 2. Wire wrapper/projections without editing completion-v1. 3. GREEN; run full shell/Node/invariant/version/no-dangling gates; explicit-path commit.
done: focused/full gates pass and `git diff --exit-code HEAD^ -- plugins/ship-flow/lib/completion-v1.sh`; model: sonnet; canonical_doc_actions: shared.

<details>
<summary>Routing and lens evidence</summary>
<!-- section:context-routing-manifest -->
```yaml
context-routing-manifest:
  domain_matches: [schema, event-saga-semantic-trigger]
  knowledge_modules: [plugins/ship-flow/references/domain-knowledge/schema.md, plugins/ship-flow/skills/architecture-lens/event-saga.md]
  required_skills: []
  stage_hints: {plan: []}
  consumer_obligations: [preserve exact schema contract, keep workflow projection non-application, route no event-saga work]
  future_provider_boundary: "Provider hints are non-authoritative; local registry is canonical."
```
<!-- /section:context-routing-manifest -->
## Context Routing Receipt
| Manifest row | Task skill mapping | Reviewer questions | Checklist row |
|---|---|---|---|
| schema | T0-T4 contract/test skills; no project-db | exact contract/rebuild | T0-T4 |
| event-saga | none; lens returned 3 SKIP | T4 scope-negative | T4 |
| provider | none; no provider configured | N/A | local registry |

### Lens Raw Verdicts
Event consumer registration, application projection wiring, and cascade choreography: SKIP; this is a workflow-local lifecycle log.
</details>

## Context Manifest
- **Skills loaded**: spacedock:ensign, ship-flow:ship-plan, superpowers:writing-plans, ship-flow:test-driven-development, ship-flow:ship-runtime-detect, reverse-recovery-audit.
- **INVARIANTS sections read**: Principles 6/8 (`INVARIANTS.md:119-317`), 9-11 (`:408-480`), 15-16 (`:561-607`).
- **Architecture docs consulted**: PRODUCT.md, ROADMAP.md, ARCHITECTURE.md, shape.md, design.md, schema knowledge module.
- **Domains touched**: schema workflow contract; event-saga semantic trigger audited SKIP.
- **Lens dispatched**: event-saga (0 FLAG, 0 PASS, 3 SKIP).
- **Lens findings integrated**: 0 integrated, 0 deferred, 0 ignored; 3 explicit SKIP.
- **Folder guidance**: files=`plugins/ship-flow/{lib,skills,references}/**`, entity artifacts -> folder_guidance_files=[]; folder_guidance_skills=[]; discovery draft empty; codex_context_boundary=root AGENTS.md/CLAUDE.md intentionally excluded from folder_guidance_files.

## Plan Report
⚠️ INCOMPLETE — this attempt crossed the unconditional 20-minute circuit while fresh semantic review was still running. The delayed review then found one short-circuiting RED-harness defect; T1/T3/T4 and the persisted ledger are repaired and later APPROVED, but that late evidence cannot promote the expired receipt. FO must run one bounded fresh receipt/gate-repair continuation.
status: partial
stage_cost: local planning + 4 read-only lens/research/review dispatches
iterations: 2 self/research-review + 1 adversarial repair round
dimensions: 7/7 late pass; circuit gate incomplete
reviewer_verdict: APPROVED after circuit expiry — non-advancing evidence
scope_anchoring: 5/5 tasks mapped; no scheduler, unrelated-stage, cross-workflow, or #21 product work
task_count: 5
model_split: 5 sonnet
skill-coverage: PASS

### Metrics
status: partial
duration_minutes: 21
iteration_count: 3
task_count: 5
verification_spec_count: 4
model_split: 5 sonnet

### Hand-off to Execute
<!-- section:hand-off-to-execute -->
- **tdd-ledger**: `docs/ship-flow/attempt-scoped-stage-circuits/tdd-ledger.jsonl`; validate with `python3 plugins/ship-flow/lib/validate-tdd-ledger.py --plan docs/ship-flow/attempt-scoped-stage-circuits/plan.md --require-ledger-jsonl docs/ship-flow/attempt-scoped-stage-circuits/tdd-ledger.jsonl`.
- **wave_order**: W0 T0 -> W1 T1 -> W2 T2 -> W3 T3 -> W4 T4.
- **critical_assumptions**: frozen completion-v1; live #21 ref resolves; Bash 3.2+/Node 18+; exact fault sequence fits three days.
- **architecture_context**: review updates ROADMAP/ARCHITECTURE, skips PRODUCT; execute changes no root canonical doc.
- **stub_flags**: none.
- **skills_needed_summary**: T0 test; T1-T2 test/best-practices/TDD; T3 test/TDD; T4 test/TDD/write-docs; four distinct lists.

#### Plan Parallelization Manifest
| Task ID | Parallel Group | Depends On | Owned Paths | Integration Owner |
|---|---|---|---|---|
| T0 | serial | none | five new tests | executer@attempt-scoped-stage-circuits |
| T1 | serial | T0 | fo-stage-attempt.sh | executer@attempt-scoped-stage-circuits |
| T2 | serial | T1 | fo-stage-attempt.sh | executer@attempt-scoped-stage-circuits |
| T3 | serial | T2 | fo-stage-attempt.sh | executer@attempt-scoped-stage-circuits |
| T4 | serial | T3 | lifecycle/skills/schema/existing tests | executer@attempt-scoped-stage-circuits |

#### Domain Acceptance Checklist
| Task | Lens | Question | Paths | Skills | Evidence |
|---|---|---|---|---|---|
| T0 | tdd | All contracts RED first? | new tests | test | named RED failures |
| T1 | contract | Exact bytes/FO authority? | helper | test,best-practices,TDD | focused RED/GREEN/digests |
| T2 | recovery | Exact fault order? | helper | test,best-practices,TDD | injected boundary matrix |
| T3 | policy/legacy | Zero dispatch and #21 exact? | helper | test,TDD | counters/hashes |
| T4 | integration | Plan/execute only? | lifecycle/skills/schema/tests | test,TDD,write-docs | focused/full gates |

#### Canonical Doc Actions Summary
| Doc | Action | Source | Rationale |
|---|---|---|---|
| ROADMAP.md | update | spec | Active prerequisite. |
| PRODUCT.md | skip | design | Internal-only. |
| ARCHITECTURE.md | update | design | Attempt authority/recovery. |
- **folder_guidance**: T0-T4 no non-root guidance; root instructions remain session context.
<!-- /section:hand-off-to-execute -->
