---
session-date: 2026-07-16
sequence: 1
first-commit: 10ce627
last-commit: 4c509e4
duration: ~1m final integration window
---

# Session Debrief — 2026-07-16 #1

C14 reached `ship` through two graph-valid transition commits after a longer recovery, RoboRev feedback, and frontmatter-authority redesign. The intentionally narrow integration window is `a7af2a4..4c509e4`; the entity ledger and stage artifacts retain the preceding recovery evidence.

## Shipped

None. C14 opened [PR #47](https://github.com/iamcxa/spacedock-workflows/pull/47) but has not merged, released, terminalized, or archived.

## Filed (backlog)

No workflow entity was filed in this integration boundary. Follow-up [issue #48](https://github.com/iamcxa/spacedock-workflows/issues/48) records canonical flat `ship-review` and same-stem shadowing support.

## Non-PR commits (workflow-only)

None surfaced. The range contains only routine `execute -> verify -> ship` dispatch transitions, which are suppressed as state churn by the debrief contract.

## Decisions

- Use **Safe NARROW**: Cases 14–31 are in scope; migration/rename, merge semantics, and authenticated provenance remain deferred to #36–#38.
- Define `stage-entry dispatch` and `completion advancement` as independent, machine-verifiable, owner-bearing contracts.
- Treat canonical frontmatter as the sole machine authority and require completion to be status-idempotent.
- Make the repair effective for the current entity and the next ship without waiting for another migration cycle.
- Use budget as a drift detector and control limit, not a byte target; engineering quality and per-token value take priority.
- Reject a one-commit `execute -> ship` squash after C14 correctly blocked the skipped edge; retain legal `execute -> verify -> ship` commits without a waiver.
- Resolve the canonical flat entity ahead of a stale same-stem folder; do not migrate or rewrite historical stage artifacts.
- Keep PR #47 CI remediation in the captain's separate lane and out of this debrief commit.

## Issues — Workflow

- GitHub Actions `invariants` failed because a fixture gives Git identity only to its initial commit; a later `git commit --amend` runs without repository or runner identity and leaves Case 3d dirty.
- GitGuardian treated two synthetic `--token=foreign` lease-fixture values as Generic CLI Secrets. A scanner-safe variable approach passed locally, then was deliberately removed and left uncommitted when CI remediation moved to a separate lane.
- Folder-first `ship-review` discovery can consume a stale same-stem `verify.md` instead of the canonical flat ledger. Filed as [#48](https://github.com/iamcxa/spacedock-workflows/issues/48); it is nonblocking for C14.
- Conductor exposes `origin` as `file://` in this worktree, so publishing required an explicit GitHub remote; the startup PR scan consequently reports `ERROR` here.
- A recursive test discovery initially included dogfood-host-only integration tests. The run was discarded and the repository's canonical standalone harness was used without changing code.

## Issues — Spacedock

None confirmed. The flat-review and same-stem-shadowing defect belongs to Ship-Flow and is tracked in #48.

## Observations

- The elapsed time came primarily from successive RoboRev findings, frontmatter-authority redesign, and repeated full shell gates rather than idle agent waiting.
- Final integration evidence was shell 109/109, Node 79/79, and C1–C15 invariants passing locally; the canonical shell gate took about 340 seconds.
- C14 blocked the illegal live-main squash immediately, proving the two transition contracts were active on the same entity.
- A developer machine's global Git identity masked a CI hermeticity gap; disposable fixture repositories should configure their own local identity.
- Budget variance should trigger engineering investigation, not byte padding, obsolete-code restoration, weakened tests, or reduced maintainability.

## What's Next

- The captain's independent CI lane owns the Git-identity fixture and GitGuardian synthetic-token remediation for PR #47.
- PR #47 remains open, `UNSTABLE`, and unmergeable until all live checks pass.
- The local C14 ledger records `pr: #47`, ship evidence, and issue #48 but remains uncommitted so the CI lane can coordinate persistence without an overlapping push.
- After CI, review, and freshness checks pass, merge PR #47, run the merge hook, mark C14 `done`, and archive issue #30's entity.
- Implement #48 independently; do not import #36–#38 into C14.
- `roborev-migration-receipt-merge-semantics` remains at `execute` with a missing recorded worktree and branch and needs separate recovery.
