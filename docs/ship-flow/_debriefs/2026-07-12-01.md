---
session-date: 2026-07-12
sequence: 1
first-commit: 0d0ca53
last-commit: cfd9d7b
duration: ~23h
---

# Session Debrief — 2026-07-12 #1

First full dogfood traversal of this workflow: entity 1 traveled
shape → design → plan → execute → verify → ship → done with 5 feedback
cycles (4 codex-gate rounds + 1 PR-CI round), landing via 3 PRs. The
pipeline's first captain gates, first cross-model EM gate-drive, and first
merge ceremony all ran in this repo.

## Shipped

- **1** `self-adoption-dogfood-bootstrap` — [#13](https://github.com/iamcxa/spacedock-workflows/pull/13) (scaffolding, merge-commit) + [#14](https://github.com/iamcxa/spacedock-workflows/pull/14) (entity) + [#15](https://github.com/iamcxa/spacedock-workflows/pull/15) (terminal state). The plugin repo now obeys its own methodology: three root canonical docs (Principle 5b flipped from WARN-skip to enforcing), the `doc-impact-gate` CI checker (config-driven couplings + anchored declaration grammar, fail-closed end to end), the harvest-vocabulary decision record, and the entity itself as the AC-3 proof that the canonical-doc sync loop runs as pipeline output.

## Filed (backlog)

- **1.1/1.2/1.3** children — filed and shipped same session (umbrella closeout per review.md; never independently advanced).
- Todos ×3 (ROADMAP `later`): `fixture-pollution-discovery-helpers`, `shape-confirm-instance-awareness`, `root-readme-stale-claims`.

## Non-PR commits (workflow-only)

None — every session commit rolled up into the three shipped PRs above
(feedback-cycle and terminal-ceremony commits included).

## Decisions

- Shape gate: confirm + captain-authored Bet (「這組跑完後 ship flow 就正式使用自身規則開發自己，且可搭配 claude or codex 的 goal 指令drive」; Codex-side `/goal` scored PARTIAL-not-failure at retro). Fork (i): native numeric-prefix confirm path over slug-only or in-pitch tooling fix.
- Design gate: approve, explicitly ratifying D3's reading of AC-2's "configured threshold" as the coupling map itself (no LOC floor — the motivating failure was a small diff).
- Verify gate: Codex 5.6 EM drives the gate review (captain directive). Cycle cap reached at round 3 → captain authorized bounded cycle-4 ("cycle-4 go"); PR-CI red (C14 helper SIGPIPE) → captain authorized bounded c5 ("c5 go") including a one-line FO direct write to the pr-merge mod.
- Ship: three-step sequence authorized in one go ("ship") — mod scope-sentence fix → scaffolding PR merged with merge commit (merge-base retirement of two historical C14 commits, no history rewrite) → entity PR squash after green CI.

## Issues — Workflow

- Discovery helpers polluted by the plugin's own test fixtures when run in-repo (`status --discover` false candidates; `discover-adopter-skills.sh` emits fixture-shaped routing) — filed as todo.
- `shape-confirm.sh`/`allocate-id.sh` instance-unawareness (ignore `id-style`, write legacy `status: sharp`, no flat-entity absorption) — filed as todo.
- C4 (plan-imported-design-dcs) ↔ C15 (verbosity caps) tension: a non-UI entity's structurally-empty imported-DCs block costs ~46 body lines; resolved per-artifact with `<details>` wrapping, worth a structural look.
- C14 exemption helper SIGPIPE/pipefail portability bug (CI-only false negatives on >47KB entities; reverse false-exempt latent in sibling pipeline) — fixed in-session (`8b66a79`), regression Cases 12-13 with >100KB fixtures.
- Dogfood assertion latency: docs-only PRs skip the full suite, so the commission PR (#12) introduced a failing dogfood assertion (pr-merge provider-scope) that only surfaced on the next plugin-touching PR — fixed in #13.

## Issues — Spacedock

- `status --discover` includes commissioned READMEs inside test-fixture trees (5 false candidates in this repo) — not filed.
- `status --set` against an entity under `_archive/` prints the resolution line but silently writes nothing (exit 0) — not filed.
- `merge guard` writes `verdict: passed` while `--validate` expects enum `PASSED` (case mismatch warning) — not filed.

## Observations

_(none recorded)_

## What's Next

- Plugin **0.9.0 release** — doc-impact-gate is the recorded minor-version candidate (review.md Release Consideration); after release: helm wiring + carlove narrow pilot (anchored in their own repos).
- Todos ×3 independently dispatchable.
- **Bet retro 2026-07-26** (ship + 2 weeks): YES / NO / PARTIAL against the Captain Bet in the archived entity.
- Codex-gate measurement pilot has its first real dataset: 4 rounds, P1s 3→2→1→0, 6/6 actionable, all FO-reproduced (usage log, 4 entries).
