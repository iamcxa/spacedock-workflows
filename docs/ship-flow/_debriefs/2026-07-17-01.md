---
session-date: 2026-07-17
sequence: 1
first-commit: 3a8f1a3
last-commit: 787cbef
duration: ~20h commit span (wall-clock across one day; includes captain-response gaps + a session-limit pause)
---

# Session Debrief — 2026-07-17 #1

A marathon single-entity run: issue #49's **issue-anchor scope-drift guard** driven `shape → ship` (PR #59 merged) by an opus First Officer. Verify was driven by the fable Science Officer/EM plus **three rounds of codex cross-vendor adversarial review**, converging on a principled line (fix real bugs; name the irreducible shell-parser residuals). The feature repeatedly caught — on its own build — the very scope-drift it exists to prevent.

## Shipped
- **5** `5-issue-anchor-scope-drift-guard` — [#59](https://github.com/iamcxa/spacedock-workflows/pull/59). At a route-back re-shape (`/shape <entity-id>` on an entity with `status ∉ {draft,sharp}`), ship-shape re-reads the immutable original GitHub issue and emits a per-AC source-diff (yq-structural, non-hollow) before re-reading the entity's own drifted artifacts; fail-visible on every degraded path; + CD5(b) intake stamping. 72 test assertions.
- _(intervening, not this session)_ **#54** — C14 accept-FO-feedback-stage-receipt landed on main from a separate session; captured here only because it fell in the un-debriefed commit range.

## Filed (backlog)
- `issue-anchor-guard-memory-fallback`, `reverse-recovery-audit-dangling-path`, `issue-anchor-guard-remaining-triggers`, `issue-anchor-guard-resolver-shell-parser-robustness` — 4 rabbit-hole todos (pitch 5).
- **gh issue [#60](https://github.com/iamcxa/spacedock-workflows/issues/60)** — ship-flow core: the human review surface is the shape/spec, not `plan.md`. `split` out of #49 (applying the guard's own logic), captain-approved.

## Non-PR commits (workflow-only)
State transitions and scaffolding that didn't belong to a PR:
- `2413487` merge: sync issue-49 ← origin/main — resolved ROADMAP + c14-archive conflicts for a clean PR base.
- `5efa121` **[C14 reword]** advance entering design — reworded the commit that had bundled a shape→design status flip into a `design:` content commit, into a sanctioned `advance:` receipt (content-identical, tree hash unchanged) to clear a C14 commit-hygiene FAIL.
- `04c81cc` ci: re-trigger doc-impact — the workflow reads the event-payload PR body and doesn't fire on `edited`; a synchronize (empty commit) was needed to re-read the body's `doc-impact: none` declaration.
- `6906f29` add-todos: shell-parser-robustness rabbit hole.
- `787cbef` chore: add .gstack/ to .gitignore — the previously-uncommitted file that had blocked the SessionStart auto-fix; now committed.

All other session commits (dispatch/advance/execute/docs cycle churn) are rolled up in PR #59.

## Decisions
- **Wedge cut (shape):** the captain cut issue #49's full proposal (3 trigger points + Q1/Q2/Q3 + 4 output verbs + 3 integration points) down to a **small-batch single wedge** — "at route-back, re-read the source ticket once" — deferring everything else. Sharpened the FO's own lean.
- **CD1 (route vocab):** captain overrode the EM's `costly_no` and chose `return` (narrowed), for enum-reuse simplicity over semantic fidelity.
- **CD5(b) (intake stamping):** applying reverse-recovery, the captain confirmed the stamping capability already exists (`instantiate-cut-project.sh` stamps `external_id`) — the gap was a missing wiring on the single-issue `/shape` path, not a rebuild — and chose to implement it (option A-minimal).
- **codex convergence:** captain approved the principled stop — fix the real bugs, name the irreducible shell-parser-robustness residuals, do not open a round 4.

## Issues — Workflow
- **C14 commit-hygiene (self-inflicted):** an external edit flipped `status: shape→design`; the FO committed it inside a `design:` content commit → C14 FAIL. Fixed via content-preserving reword (reset + cherry-pick; branch unpushed, low risk). Lesson: status transitions must always ride a `dispatch:`/`advance:` receipt, never a content commit.
- **guard-before-Intake flow bug (codex r3 P1):** the guard was wired *before* Intake, so a brand-new free-text `/shape` would hit the resolver's "entity path not found" BLOCK. Fixed by gating the guard after Intake's existing-entity match (realigns with premise A1).
- **CD5(b) silent-drop:** an imported design constraint reached plan as a constraint row but had no task/DC/test/deferral — a silent scope-shrink, exactly what this entity guards against; caught by the fable EM at the verify gate.
- **doc-impact gate on `edited` (reclassified from Spacedock — NOT a framework bug):** the ship-flow doc-impact workflow lives in this repo and reads the event-payload PR body; `pull_request` default types exclude `edited`, so a PR-body-only fix needs a synchronize to re-run. Standard GitHub Actions behavior, not a bug — noted as DX friction only.
- **SessionStart auto-fix blocked by a dirty tree (reclassified from Spacedock — NOT a bug):** the FO auto-fix correctly refuses to mutate state while the working tree is dirty; the blocker was an unrelated pre-existing `M .gitignore` (now committed as `787cbef`, unblocking entity 5's done+archive).

## Issues — Spacedock
- **Ensign completion signal targets an unreachable `team-lead` in the non-TeamCreate Claude dispatch path** — filed as [spacedock-dev/spacedock#523](https://github.com/spacedock-dev/spacedock/issues/523). Every stage worker's `SendMessage(to="team-lead", "Done: …")` failed; completion was recognized only via `task_notification`. Benign (task_notification covers it) but reproducible across ~8 dispatches; the issue asks maintainers whether direct-`Agent` dispatch (no `TeamCreate`) is a supported path.

## Observations
- **The guard worked on its own creation (dogfood, 5 moments):** it caught two silent scope-drops inside its own build (CD5(b), the residual set); the captain used its `split` logic to route the review-surface insight to a separate issue (#60) rather than absorb it; reverse-recovery revealed CD5(b) was wiring not rebuild; and when codex kept surfacing shell-parser edges, we drew a principled line right where "verification itself starts to scope-drift" — the exact failure the guard names.
- **Verification loops can drift too.** codex found real bugs across 3 rounds, but rounds 2–3 trended toward the irreducible "a shell script cannot perfectly parse adversarial YAML/Markdown" class. Separating "real bug (fix)" from "irreducible residual (name + accept)" is what converges an adversarial review; chasing the latter is gold-plating.
- **Human review surface = spec, not plan** (→ #60; saved as memory `ship-flow-human-reviews-spec-not-plan`). The FO briefly offered the captain a `plan.md` review; the captain corrected it — humans review the shape/spec (what & why); plan/execute are agent-facing.

## What's Next
- **entity 5 done+archive** — now unblocked (`787cbef` committed the dirty `.gitignore`); the next FO boot's auto-fix (or a manual `done + archive` follow-up PR, per this repo's `#51 c14-done-archive` convention) should terminalize it from `ship`.
- **#60** — ship-flow-core review-surface discipline; a new-session prompt was handed to the captain.
- **Deferred (pitch 5 rabbit-holes):** memory-fallback, remaining-triggers (cycle-3 / child-creation), shell-parser-robustness (structural scalar validation, Markdown-aware AC parse, tombstone-abort), AC-N issue-body convention. Minor NIT: design.md/plan.md still say "before Intake" (code moved it after) — reconcile at doc-sync.
- **Pre-existing:** `shape-confirm-instance-awareness` (shape gate, 0/3 ACs); `roborev-migration-receipt-merge-semantics` (execute, orphan worktree from prior sessions).
