---
session-date: 2026-07-16
sequence: 2
first-commit: e09eaf8
last-commit: 195f942
duration: ~45m (commit span; the CI lane handed off from #1)
---

# Session Debrief — 2026-07-16 #2

Continuation of #1's "captain independent CI lane". Fixed both PR #47 CI blockers, merged the PR, and closed issue #30. The `a7af2a4..4c509e4` C14 integration from #1 landed on `main` via merge commit `195f942`.

## Shipped

- **c14** `c14-fo-dispatch-contract` — [#47](https://github.com/iamcxa/spacedock-workflows/pull/47) (merge `195f942`). Separate First Officer stage-entry from status-idempotent completion as two machine-verifiable contracts with frontmatter as the sole state authority; this session repaired its CI and merged, closing #30.

## Filed (backlog)

None. No workflow entity was filed this session.

## Non-PR commits (workflow-only)

State transitions and scaffolding that don't belong to a PR:

- None surfaced. The CI fix `e09eaf8` landed inside PR #47 (rolled up in the shipped PR above); `73e0007` is #1's own debrief commit.

## Decisions

- GitGuardian incident `34883744` (`--token=foreign` lease-fixture literal) was a confirmed false positive; the captain resolved it on the GitGuardian dashboard.
- Merged PR #47 despite the cosmetic red GitGuardian check: `main` has no branch protection, GitGuardian is not a required check (`mergeStateStatus: UNSTABLE` = mergeable), and dashboard resolution does not retroactively flip a completed check green.
- Kept the CI remediation forward-only (no history rewrite / force-push): the in-range literal in `10ce627` was left in history and handled by dashboard resolution rather than rewriting shipped commits.

## Issues — Workflow

- `invariants` failed only in `test-advance-stage.sh`: the fixture set Git identity on the initial commit alone, so later `git commit --amend` calls failed on the identity-less CI runner (Cases 3/7/8). Fixed by setting repo-local `user.email`/`user.name` right after `git init` (suite convention `test-allocate-id.sh`); verified by reproducing the identity-less runner locally (`GIT_CONFIG_GLOBAL=/dev/null` + `user.useConfigOnly=true`), 103/103 cases pass, and confirmed green on the live runner.
- GitGuardian flagged the synthetic `--token=foreign` fixture value. Routed it through `FOREIGN_LEASE_FIXTURE_TOKEN` (ggshield confirms the file is scanner-clean). Because the literal persists in the in-range commit `10ce627`, the completed check did not auto-flip; cleared by dashboard false-positive resolution.

## Issues — Spacedock

- Post-merge reconciliation did not run automatically. After a direct `gh pr merge`, the C14 entity on `main` remains `status: ship` (not `done`, not archived), `completed:` empty, and ROADMAP is not updated — the merge hook / pr-merge mod only fires through the First Officer pr-merge flow, not a direct GitHub merge. Not filed; noted for the next FO session.
- Conductor exposes `origin` as an invalid `file://` in this worktree, so `git push origin` fails with `no path specified` (reproduced via plain git, absolute-path git, and raw `rtk proxy`); publishing to GitHub required Conductor's own sync. Environmental, not filed.
- During the session Conductor switched the workspace branch multiple times (`continue-top-priority-item-v1` → `confirm-merge`) and left it on stale `a7af2a4` (behind the merge); the debrief had to be based on `origin/main`. Environmental, not filed.

## Observations

- `ggshield secret scan commit-range <merge-base>..HEAD` was the decisive local tool: it proved the GitGuardian literal lived in an in-range commit, so a forward-only file fix could not clear a full-range PR scan — which is why dashboard resolution (not just the variable fix) was required.
- The #1 → CI-lane handoff was clean, but the shared Conductor worktree was actively contended (a sibling debrief commit and branch switches mid-session); `git -C` plus HEAD-stability monitoring prevented stepping on it.
- A developer machine's global Git identity and git's implicit OS identity both masked the CI hermeticity gap; `user.useConfigOnly=true` is needed to reproduce the identity-less runner locally.

## What's Next

- **C14 post-merge reconciliation** (next FO session): mark the entity `done`, set `completed`, archive issue #30's entity, and update ROADMAP — the direct merge skipped the merge hook.
- `roborev-migration-receipt-merge-semantics` remains at `execute` with a missing recorded worktree/branch and needs separate recovery.
- Open follow-ups remain independent: #36 / #37 / #38 (deferred C14 hardening) and #48 (canonical flat `ship-review` without same-stem shadowing).
