# Issue-Anchor Scope-Drift Guard — Implementation Plan

**Goal:** Ship `plugins/ship-flow/_mods/issue-anchor-guard.md` as a wired mod (`## Hook: pre-shape` + extractable resolver) that re-reads the entity's immutable original tracker issue on re-shape and emits a five-field, per-AC source-diff YAML before ship-shape lets the model re-summarize its own drifted artifacts — pinned end-to-end by `test-issue-anchor-guard.sh`, not asserted by prose.
**Architecture:** CD1-CD5 (design.md Reconciliation, D1-D5) all resolve toward one invariant: the done-check is a shell-testable YAML artifact, never a self-report. Detection is `status:`-primary (covers flat-file entities), folder-artifacts secondary. Route vocabulary reuses the existing SO/EM `proceed`/`narrow`/`return` triad — no new enum values, no `science-officer-em.md` change.
**Tech stack:** bash 3.2+, `gh` CLI (PATH-stubbed in tests), Markdown mod/skill files, shellcheck.

<details>
<summary>Plan Imported Design DCs — generated by import-design-dcs.sh</summary>

## Plan Imported Design DCs

<!-- section:plan-imported-design-dcs -->
<!-- Generated by plugins/ship-flow/lib/import-design-dcs.sh from entity's ### Hand-off to Plan block. Do not hand-edit; rerun the script if the source hand-off changes. -->

### Imported design_constraints

| # | Type | Assertion | Rationale (D{N}) | Source artifact |
|---|---|---|---|---|
| 1 | contract | "Guard emits verdict as exactly one of proceed/narrow/return (CD1 vocabulary lock, no new SO/EM route values); re-anchor maps to return, narrowly defined as 'the original goal is already met by existing capability — close or defer this entity'; split stays out of scope this round." | D1 | docs/ship-flow/5-issue-anchor-scope-drift-guard/design.md |
| 2 | contract | "Ship plugins/ship-flow/_mods/issue-anchor-guard.md as a wired mod: Hook heading + extractable resolver block, invoked from a pinned section block in ship-shape/SKILL.md before Intake — not an inline prose-only SKILL section with a string-assertion test." | D2 | docs/ship-flow/5-issue-anchor-scope-drift-guard/design.md |
| 3 | contract | "Resolver auto-detects re-shape via status:∉{draft,sharp} (PRIMARY) OR folder artifacts (secondary, design.md/plan.md/execute.md/verify.md/review.md present); default-on, escape hatch is explicit --skip-issue-anchor-guard." | D3 | docs/ship-flow/5-issue-anchor-scope-drift-guard/design.md |
| 4 | schema-contract | "Source-diff YAML carries schema_version/entity_id/issue_ref/issue_fetched_at/original_issue_acs[] (per-AC object with met_by_existing_capability bool)/current_scope_delta[]/scope_subset_of_issue/goal_still_unmet (derived)/verdict/rationale; non-hollow rule enforced." | D4 | docs/ship-flow/5-issue-anchor-scope-drift-guard/design.md |
| 5 | contract | "Empty-string issue: treated as absent (no_issue_anchor + captain_prompt, never a fake diff); gh failure exits non-zero with a visible error, never a fake-empty AC list; shape-confirm bounded intake-stamping carries issue:/tracker: forward." | D5 | docs/ship-flow/5-issue-anchor-scope-drift-guard/design.md |

**Wave assignment**: D1/D2/D3/D5 anchor to T2 (mod/resolver); D4 anchors to T1 (test schema pin) and T2 (emission); D2's SKILL wiring half anchors to T3. No UI targets — non-UI entity.
<!-- /section:plan-imported-design-dcs -->

</details>

## Plan Output

### Research Summary

No dispatched research — design.md's Implementation Sketch already names exact files, line-insertion points, and byte-scale estimates (~320 net LoC total), so Step 2 research is skipped per Shape Up appetite discipline (all facts came from a direct codebase read, cited below). Self-review pass: 1 (no contradictions found against current file state — re-validated at Step 1.5 below). No cross-review dispatch was run standalone; this plan is dispatched by the FO for review inline with entity progression.

### Assumption Re-validation (Step 1.5)

- shape.md A1/A2/A3 file:line citations re-read: `workflow-template.yaml` feedback-to only on verify (confirmed — `docs/ship-flow/README.md` `states:` block, this session), ship-shape is FO-direct (confirmed, no ensign indirection for shape stage), `entity-body-schema.yaml` issue:/tracker: fields exist (confirmed at `hand_off_to_design` schema). No contradictions.
- `plugins/ship-flow/_mods/science-officer-em.md:110` re-read this session: `route` is one of `proceed`, `narrow`, `return`, `block`, or `costly_no` — CD1's enum-reuse premise holds exactly.
- Critical precondition: T2/T3 must not add `re-anchor`/`split` anywhere in `science-officer-em.md` or its five test files; T4's `git diff --check` proves this.

### Size Re-evaluation

Appetite (small-batch, 2-3 days) holds — design's Artifacts Changed table lists 4 touched files (mod new, SKILL.md edit, test new, doc-coupling-map.yaml edit) plus 2 root-canonical touches (ROADMAP, ARCHITECTURE) from this plan's own Canonical Doc Actions below; `docs/ship-flow/README.md`'s optional mods-index line is skipped (confirmed empirically — no mods index exists there). 6 total files, all within a single plugin subdirectory family; no size upgrade.

### Verification Spec

| DC | Verify Procedure | Expected |
|---|---|---|
| DC-1 | `test -f plugins/ship-flow/_mods/issue-anchor-guard.md && grep -q '^## Hook: pre-shape' plugins/ship-flow/_mods/issue-anchor-guard.md` | Mod file exists with the pinned Hook heading. |
| DC-2 | extract `# issue-anchor-guard-resolver:start`/`:end` block, `bash -n` on the extract | Resolver block is valid, syntactically-checkable shell. |
| DC-3 | fixture `fx-reshape-with-issue` (design.md+plan.md+`issue:"#49"`) → run resolver | Writes `.context/ship-flow/source-diff-<id>.yaml` with all 5 CD4 fields; `original_issue_acs[]` non-empty, each row carries `met_by_existing_capability`. |
| DC-4 | fixture `fx-fresh-shape` (no design.md, `status: sharp`) → run resolver | Exits 0, writes `guard_required: false`, no YAML source-diff. |
| DC-5 | fixture `fx-reshape-no-issue` (design.md present, no `issue:`) → run resolver | Exits 0, writes `no_issue_anchor: true` + `captain_prompt`, no fake diff. |
| DC-6 | `grep -q '<!-- section:issue-anchor-guard -->' plugins/ship-flow/skills/ship-shape/SKILL.md` and marker precedes `### Intake` by line number | Pinned wiring block present before Intake. |
| DC-7 | fixture with crafted `verdict: proceed` + `scope_subset_of_issue: false` → run resolver's own validation step | Resolver/test rejects the combination (non-hollow rule); `verdict` value is always one of proceed/narrow/return. |
| DC-8a | fixture `fx-empty-issue` (`issue: ""`) → run resolver | Treated identically to DC-5 (absent), not as a truthy anchor. |
| DC-8b | fixture with PATH-stubbed failing `gh` (exit 1) → run resolver | Resolver exits non-zero, stderr carries a captain-visible message; no YAML written (never a fake-empty AC list). |
| DC-9 | `test-shape-confirm.sh` DC-5.1-1..3: proposal with `pitch.issue`+`pitch.tracker` → `shape-confirm.sh --layout=folder\|flat` | Pitch entity frontmatter carries `issue: "<ref>"` and `tracker: <gh\|linear>`; shaped-child entities do NOT (pitch-only scope); proposal without `pitch.issue` emits neither (unchanged behavior). |

### Canonical Doc Actions

| Doc | Action | Source | Rationale |
|---|---|---|---|
| ROADMAP.md | update | plan | Entity already listed under `## Next`; move to `## Now` — plan stage is active-work start (c14 precedent). |
| PRODUCT.md | skip | design | Internal ship-flow pipeline-quality mechanism (re-shape scope-drift guard), analogous to the existing "Mechanical CI gates" quality-domain row rather than a new user-facing capability domain; same rationale class as c14's PRODUCT.md skip. |
| ARCHITECTURE.md | update | design | New component (`_mods/issue-anchor-guard.md`) wired into ship-shape's Pre-Intake boundary; record the CD1-CD5 resolution as a decisions-table row, esp. the enum-reuse boundary (no new SO/EM route values). |
| README.md (root) | skip | plan | Root README links only `_mods/contribution-contract.md` (it ships a captain-facing `CONTRIBUTING.md` workflow); this guard is an internal ship-shape re-entry safeguard with no new contributor-facing document. |

<details>
<summary>Task-to-scope anchoring</summary>

### Scope Anchoring

| Task | Shape/design mapping |
|---|---|
| T1 | Acceptance Outcome (source-diff shown before re-shape); DC-1/DC-2/DC-3/DC-4/DC-5/DC-6/DC-7/DC-8a/DC-8b; D1/D3/D4/D5 |
| T2 | Acceptance Outcome; DC-1/DC-2/DC-3/DC-4/DC-5/DC-7/DC-8a/DC-8b; D1/D3/D4/D5 |
| T3 | Acceptance Outcome (guard runs at re-shape, not just documented); DC-6; D2 |
| T4 | Canonical Doc Actions; no shape DC (meta) |
| T5 | CD5(b) bounded intake-stamping (added post-T4 to close the silent-drop doctrine gap — see Addendum below); DC-9; D5 |

</details>

### Plan

#### T1 — Author test-issue-anchor-guard.sh (RED authoring)
task_id: T1
layer: L5
wave: W1
files: `plugins/ship-flow/lib/__tests__/test-issue-anchor-guard.sh`
read_first: design.md CD2 "Test implications" + CD4 schema + Reconciliation per-AC delta + CD5 cases; `plugins/ship-flow/lib/__tests__/test-contribution-contract.sh:26-33` (extractor pattern); `plugins/ship-flow/lib/__tests__/test-check-pr-mergeable.sh:49-71` (gh PATH-stub pattern)
skills_needed: [test, best-practices, test-driven-development]
reviewer_questions: [{lens: contract, question: "Does the test pin every CD2 resolver assertion (mod+Hook, resolver bash -n, 3 fixtures, SKILL.md wiring, verdict enum) plus CD4 per-AC fields and CD5 cases (empty-string issue:, gh-failure, no-issue) without asserting on implementation internals?", affected_path_family: "plugins/ship-flow/lib/__tests__/test-issue-anchor-guard.sh", evidence_required: "RED run showing every assertion fails for the right reason (missing mod/SKILL section), not a script error"}]
tdd_contract:
  red_command: "bash plugins/ship-flow/lib/__tests__/test-issue-anchor-guard.sh"
  expected_red_failure: "All assertions fail: plugins/ship-flow/_mods/issue-anchor-guard.md does not exist (DC-1/2/3/4/5/7/8a/8b), and ship-shape/SKILL.md has no <!-- section:issue-anchor-guard --> block (DC-6) — RED proves real absence, not a shell syntax error."
  green_command: "bash plugins/ship-flow/lib/__tests__/test-issue-anchor-guard.sh"
  refactor_check: "bash -n plugins/ship-flow/lib/__tests__/test-issue-anchor-guard.sh"
parallel_group: serial; depends_on: []; owned_paths: [plugins/ship-flow/lib/__tests__/test-issue-anchor-guard.sh]; integration_owner: executer@5-issue-anchor-scope-drift-guard
steps: 1. Copy the awk resolver-extractor and gh PATH-stub helpers from the two cited test files. 2. Build 3 entity fixtures under `$TMP_DIR`: `fx-reshape-with-issue` (index.md `issue:"#49"`, shape.md, design.md), `fx-fresh-shape` (index.md `status: sharp` + shape.md only), `fx-reshape-no-issue` (design.md present, no `issue:`); add `fx-empty-issue` (`issue: ""`) and a failing-gh fixture for DC-8a/DC-8b. 3. Add DC-1/DC-2 (mod existence, Hook heading, `bash -n` on extracted resolver). 4. Add DC-3/DC-4/DC-5/DC-8a/DC-8b (fixture-based resolver invocations). 5. Add DC-6 (SKILL.md pinned-section + line-order check) and DC-7 (verdict enum + non-hollow-rule rejection case). 6. Run RED, capture output, commit.
done: test exits non-zero with every assertion failing on absence (not a bash parse error); `bash -n` on the test file is clean.
model: sonnet; canonical_doc_actions: shared table.

#### T2 — Implement issue-anchor-guard.md mod + resolver
task_id: T2
layer: L5
wave: W2
files: `plugins/ship-flow/_mods/issue-anchor-guard.md`
read_first: T1 assertions; design.md `## Implementation Sketch → New file` section; `plugins/ship-flow/_mods/contribution-contract.md` (frontmatter + Hook + resolver-block structural pattern)
skills_needed: [test, best-practices, test-driven-development]
reviewer_questions: [{lens: contract, question: "Does the resolver implement CD3 detection order (status PRIMARY), the CD4 per-AC schema with the non-hollow rule, and the CD5 empty-string/gh-failure/no-issue paths exactly as D1-D5 specify, without touching science-officer-em.md's route enum?", affected_path_family: "plugins/ship-flow/_mods/issue-anchor-guard.md", evidence_required: "T1's DC-1/2/3/4/5/7/8a/8b GREEN; `git diff --stat` shows science-officer-em.md untouched"}]
tdd_contract:
  red_command: "bash plugins/ship-flow/lib/__tests__/test-issue-anchor-guard.sh"
  expected_red_failure: "DC-1/2/3/4/5/7/8a/8b fail before this task lands; DC-6 (SKILL.md wiring) stays red until T3."
  green_command: "bash plugins/ship-flow/lib/__tests__/test-issue-anchor-guard.sh"
  refactor_check: "awk '/# issue-anchor-guard-resolver:start/{f=1;next}/# issue-anchor-guard-resolver:end/{exit}f' plugins/ship-flow/_mods/issue-anchor-guard.md | bash -n"
parallel_group: w2-mod; depends_on: [T1]; owned_paths: [plugins/ship-flow/_mods/issue-anchor-guard.md]; integration_owner: executer@5-issue-anchor-scope-drift-guard
steps: 1. Frontmatter + `## Hook: pre-shape` + `## Invocation` (skip only on fresh-shape or `--skip-issue-anchor-guard`). 2. Resolver: resolve entity dir; detect re-shape (status PRIMARY, folder secondary); non-re-shape → `guard_required: false`, exit 0; read `issue:`/`tracker:`, empty-string treated as absent → `no_issue_anchor: true` + `captain_prompt`, exit 0; `tracker: gh` → `gh issue view <n> --json title,body,labels`, parse AC-N lines into per-AC rows with `met_by_existing_capability`; gh failure → non-zero exit + captain-visible stderr, no partial YAML; else compute `scope_subset_of_issue`/derived `goal_still_unmet`/`verdict` per non-hollow rule, emit `.context/ship-flow/source-diff-<id>.yaml`. 3. `## Schema (locked)` skeleton (per-AC object form) + `## Rationale + References` (`science-officer-em.md:110`). 4. Iterate against T1 until DC-1/2/3/4/5/7/8a/8b GREEN. 5. Commit only this file.
done: T1's DC-1/2/3/4/5/7/8a/8b GREEN; science-officer-em.md diff-clean.
model: sonnet; canonical_doc_actions: shared table.

#### T3 — Wire ship-shape SKILL.md + doc-coupling-map.yaml row
task_id: T3
layer: L5
wave: W2
files: `plugins/ship-flow/skills/ship-shape/SKILL.md`, `plugins/ship-flow/references/doc-coupling-map.yaml`
read_first: T1 DC-6; design.md `## Implementation Sketch → Changed file` + `### Doc-coupling row`; current `ship-shape/SKILL.md` Intake header (~line 49) and References block (~line 585)
skills_needed: [write-docs, test]
reviewer_questions: [{lens: contract, question: "Is the Pre-Intake section pinned with the exact section markers before Intake (not after), does References cite the mod path, and does the new doc-coupling-map.yaml row keep the contribution-contract bidirectional gate honest for this source/doc pair?", affected_path_family: "plugins/ship-flow/skills/ship-shape/**, plugins/ship-flow/references/doc-coupling-map.yaml", evidence_required: "T1 DC-6 GREEN; doc-coupling-map.yaml schema_version unchanged, new row present"}]
tdd_contract:
  red_command: "bash plugins/ship-flow/lib/__tests__/test-issue-anchor-guard.sh"
  expected_red_failure: "DC-6 (pinned <!-- section:issue-anchor-guard --> block + resolver invocation before Intake) fails before this task lands."
  green_command: "bash plugins/ship-flow/lib/__tests__/test-issue-anchor-guard.sh && bash plugins/ship-flow/lib/__tests__/test-contribution-contract.sh"
  refactor_check: "bash -n plugins/ship-flow/lib/__tests__/test-issue-anchor-guard.sh"
parallel_group: w2-skill; depends_on: [T1]; owned_paths: [plugins/ship-flow/skills/ship-shape/SKILL.md, plugins/ship-flow/references/doc-coupling-map.yaml]; integration_owner: executer@5-issue-anchor-scope-drift-guard
steps: 1. Insert `### Pre-Intake: Issue-Anchor Guard` immediately before `### Intake`, wrapped in `<!-- section:issue-anchor-guard --> ... <!-- /section:issue-anchor-guard -->` per design.md's exact text. 2. Add one References line citing the mod path. 3. Add the doc-coupling-map.yaml row (`name: issue-anchor-guard`, srcGlobs/docPaths, `directions: [source-to-doc, doc-to-source]`). 4. Run T1; confirm DC-6 GREEN; run `test-contribution-contract.sh` to confirm the new row doesn't break the production-map assertion. 5. Commit both files together.
done: T1 fully GREEN (all 9 DCs); `test-contribution-contract.sh` unaffected.
model: sonnet; canonical_doc_actions: shared table.

#### T4 — Canonical doc actions (ROADMAP move + ARCHITECTURE decision row)
task_id: T4
layer: meta
wave: W3
files: `ROADMAP.md`, `ARCHITECTURE.md`
read_first: T2/T3 diffs; Canonical Doc Actions table above
TDD: skip -- docs-only canonical synchronization; existing check-invariants.sh section-tag + patch-map.sh CAS gates are the alternate validation.
parallel_group: serial; depends_on: [T2, T3]; owned_paths: [ROADMAP.md, ARCHITECTURE.md]; integration_owner: executer@5-issue-anchor-scope-drift-guard
steps: 1. Via `lib/patch-map.sh`, move `5-issue-anchor-scope-drift-guard` from ROADMAP.md `## Next` to `## Now`. 2. Add an ARCHITECTURE.md `<!-- section:decisions -->` row summarizing the CD1-CD5 resolution (enum-reuse boundary; wired mod; status-primary detection; per-AC schema; anchor-availability cases). 3. Run `CI=true bash plugins/ship-flow/bin/check-invariants.sh` full suite + `git diff --check`.
done: ROADMAP/ARCHITECTURE updated via section-tag CAS; check-invariants.sh clean except the pre-existing, out-of-scope C14 finding on commit `cef479ff` (predates this plan; a design-stage commit-message format issue, not this entity's code).
model: sonnet; canonical_doc_actions: shared table.

#### Addendum (post-T4 route-back): T5 — shape-confirm.sh bounded intake-stamping

The original T1-T4 plan (above) shipped the issue-anchor-guard mod itself but never
implemented CD5(b) — the "future entities are born anchored" half of D5. A verify-stage
review surfaced this as a silent-drop doctrine violation: CD5(b) was a named, captain-adjudicated
decision with no task, no DC, and no test backing it. This addendum adds T5 to close that gap;
T1-T4's own Plan Report/Metrics/Hand-off-to-Execute below are left as the historical record of
that round and are not rewritten.

#### T5 — shape-confirm.sh bounded intake-stamping (CD5(b))
task_id: T5
layer: L5
wave: W4 (post-T4, route-back addition)
files: `plugins/ship-flow/lib/shape-confirm.sh`, `plugins/ship-flow/lib/__tests__/test-shape-confirm.sh`, `plugins/ship-flow/skills/ship-shape/SKILL.md`
read_first: design.md Reconciliation CD5(b) + D5 Captain Decision; `plugins/ship-flow/lib/instantiate-cut-project.sh` external_id/external_project stamping pattern (the mirrored precedent); `test-shape-confirm.sh` DC-129.4 pre_mortem-emission block (the closest existing conditional-optional-field precedent, incl. its pitch-only-scope assertion)
skills_needed: [test, best-practices, test-driven-development]
reviewer_questions: [{lens: contract, question: "Does shape-confirm.sh stamp issue:/tracker: only when both are present in the proposal (absent pitch.issue -> no stamping, unchanged behavior), only onto the pitch entity (never a shaped-child), for both folder and flat layouts, without touching the guard mod, AC-N parsing, or Linear/external_id handling?", affected_path_family: "plugins/ship-flow/lib/shape-confirm.sh, plugins/ship-flow/lib/__tests__/test-shape-confirm.sh", evidence_required: "RED run showing the new DC-5.1 assertions fail on the pre-change binary (real absence, not a shell error); GREEN after the two-line stamping addition; test-issue-anchor-guard.sh + shape-confirm's own full suite unaffected"}]
tdd_contract:
  red_command: "bash plugins/ship-flow/lib/__tests__/test-shape-confirm.sh"
  expected_red_failure: "DC-5.1-1a/1b (folder index.md issue:/tracker:) and DC-5.1-2 (flat .md issue:/tracker:) fail — shape-confirm.sh has zero issue/tracker handling; DC-5.1-1c/DC-5.1-3 (negative cases) pass trivially since nothing is stamped."
  green_command: "bash plugins/ship-flow/lib/__tests__/test-shape-confirm.sh"
  refactor_check: "bash -n plugins/ship-flow/lib/shape-confirm.sh && bash -n plugins/ship-flow/lib/__tests__/test-shape-confirm.sh"
parallel_group: serial; depends_on: [T2] (reads the CD5(b) decision T2 did not implement); owned_paths: [plugins/ship-flow/lib/shape-confirm.sh, plugins/ship-flow/lib/__tests__/test-shape-confirm.sh, plugins/ship-flow/skills/ship-shape/SKILL.md]; integration_owner: executer@5-issue-anchor-scope-drift-guard
steps: 1. Add DC-5.1 fixture + assertions to `test-shape-confirm.sh` (proposal with `pitch.issue`+`pitch.tracker` → folder+flat stamping; pitch-only scope; absent-field no-op); confirm RED. 2. Extract `PITCH_ISSUE`/`PITCH_TRACKER` from `.pitch.issue // ""` / `.pitch.tracker // ""` (mirrors the existing `PITCH_ANSWERS_DENSITY` extraction one line above); emit `issue: "<ref>"` / `tracker: <val>` conditionally into the pitch-only heredoc for both folder (`index.md`) and flat (`.md`) layouts, mirroring `instantiate-cut-project.sh`'s `external_id`/`external_project` stamping. 3. Add an Intake-section note to `ship-shape/SKILL.md` instructing the composer to populate `pitch.issue`/`pitch.tracker` when the `/shape` directive references a tracker issue (`#N` or URL). 4. Confirm GREEN; run `test-issue-anchor-guard.sh`, `test-doc-impact-gate.sh`, `test-contribution-contract.sh` unaffected; run full `check-invariants.sh`.
done: DC-5.1-1..3 GREEN; `test-shape-confirm.sh` full suite green; `test-issue-anchor-guard.sh` 32/32 unaffected; `git diff --check` clean; no change to `plugins/ship-flow/_mods/issue-anchor-guard.md`, no AC-N parsing added, no Linear/`external_id` handling touched.
model: sonnet; canonical_doc_actions: none (docs-only prose addition to ship-shape/SKILL.md is covered by this task's own DC, not a separate ROADMAP/ARCHITECTURE row — those already carry the CD1-CD5 decision summary from T4 and are out of this addendum's bounded scope).

<details>
<summary>Context routing manifest and receipt</summary>

<!-- section:context-routing-manifest -->
```yaml
context-routing-manifest:
  domain_matches: []
  knowledge_modules: []
  required_skills: []
  stage_hints: {plan: []}
  consumer_obligations: [preserve science-officer-em.md route enum unchanged, treat design.md Reconciliation D1-D5 as authoritative over the earlier CD trade-off tables where they differ]
  future_provider_boundary: "No domain registry match; local mod/skill convention is the routing source of truth for this entity."
```
<!-- /section:context-routing-manifest -->

## Context Routing Receipt

| Manifest row | Plan task skill mapping | Reviewer questions | domain_acceptance_checklist row |
|---|---|---|---|
| none (no domain match) | T1-T3 use test/best-practices/write-docs only; no project-db/fmodel/UI skill | T1-T3 contract questions above | T1-T3 rows below |

</details>

## Context Manifest

- **Skills loaded**: ship-plan, ship-flow:test-driven-development (fallback TDD contract owner; `superpowers:writing-plans` not invoked standalone this pass — task shape follows the c14/contribution-contract precedent directly).
- **INVARIANTS sections read**: Principle 5 (`plugins/ship-flow/INVARIANTS.md:96`), Principle 6 (`:119`), Principle 8 (`:288`), Principle 15 (`:561`).
- **Architecture docs consulted**: PRODUCT.md, ROADMAP.md, ARCHITECTURE.md, design.md (Reconciliation + Captain Decisions + Hand-off to Plan).
- **Domains touched**: none — no domain-registry trigger; shell mod + SKILL.md wiring within `plugins/ship-flow/`.
- **Lens dispatched**: none (no tag/customer/event-saga trigger match).
- **Lens findings integrated**: 0 integrated, 0 deferred, 0 ignored.
- **Folder guidance**: files=`plugins/ship-flow/{_mods,skills,lib/__tests__,references}/**`, root docs → folder_guidance_files=[]; folder_guidance_skills=[]; codex_context_boundary=root AGENTS.md/CLAUDE.md intentionally excluded from folder_guidance_files.

## Plan Report

status: passed
stage_cost: local planning, no dispatched research/review (design.md pre-supplied the file-level research)
iterations: 1 self-review
dimensions: requirement, completeness, dependencies, placeholders, signatures, minimality, TDD, line anchors, design import, context all pass
reviewer_verdict: PROCEED
scope_anchoring: 4/4 tasks mapped; no unmapped task
task_count: 4
model_split: 4 sonnet implementation tasks

### Metrics

status: passed
duration_minutes: 35
iteration_count: 1
task_count: 4
verification_spec_count: 9
model_split: 4 sonnet

### Hand-off to Execute

<!-- section:hand-off-to-execute -->
- **wave_order**: W1 T1 -> W2 (T2 ∥ T3) -> W3 T4.
- **critical_assumptions**: shape A1/A2/A3 re-validated clean at Step 1.5; `science-officer-em.md:110` route enum re-confirmed unchanged as of this plan.
- **architecture_context**: update ROADMAP.md (Next→Now) and ARCHITECTURE.md (decisions row); PRODUCT.md and root README.md skip — see Canonical Doc Actions.
- **canonical_doc_actions_summary**: ROADMAP update / PRODUCT skip / ARCHITECTURE update / README(root) skip — full table above.
- **stub_flags**: none — no `stub|fake|placeholder|v1.*only|wired only for` language in any task.
- **skills_needed_summary**: T1/T2 test+best-practices+TDD; T3 write-docs+test; T4 none (docs-only, TDD-skip). Three distinct lists across heterogeneous tasks.
- **domain_acceptance_checklist**:

  | Task ID | Verify Lens | Reviewer Question | Affected Path Family | Required Skills | Evidence Required |
  |---|---|---|---|---|---|
  | T1 | contract | Does the test pin every CD2/CD4/CD5 assertion without asserting internals? | lib/__tests__ | test, best-practices, TDD | RED run, right-reason failures |
  | T2 | contract | Does the resolver honor CD3 detection order, CD4 per-AC schema, CD5 paths, without touching the route enum? | _mods/issue-anchor-guard.md | test, best-practices, TDD | DC-1/2/3/4/5/7/8a/8b GREEN; enum diff-clean |
  | T3 | contract | Is the Pre-Intake section pinned before Intake with a correct doc-coupling row? | skills/ship-shape, references | write-docs, test | DC-6 GREEN; contribution-contract test unaffected |
  | T4 | architecture | Do canon updates match the Canonical Doc Actions table exactly? | ROADMAP.md, ARCHITECTURE.md | write-docs | section-tag CAS diff, full check-invariants.sh |

- **context-routing-manifest**: see standalone block above; no domain match, local convention is authoritative.
<!-- /section:hand_off_to_execute -->

| Task ID | Parallel Group | Depends On | Owned Paths | Integration Owner |
|---|---|---|---|---|
| T1 | serial | — | `plugins/ship-flow/lib/__tests__/test-issue-anchor-guard.sh` | executer@5-issue-anchor-scope-drift-guard |
| T2 | w2-mod | T1 | `plugins/ship-flow/_mods/issue-anchor-guard.md` | executer@5-issue-anchor-scope-drift-guard |
| T3 | w2-skill | T1 | `plugins/ship-flow/skills/ship-shape/SKILL.md`; `plugins/ship-flow/references/doc-coupling-map.yaml` | executer@5-issue-anchor-scope-drift-guard |
| T4 | serial | T2,T3 | `ROADMAP.md`; `ARCHITECTURE.md` | executer@5-issue-anchor-scope-drift-guard |
