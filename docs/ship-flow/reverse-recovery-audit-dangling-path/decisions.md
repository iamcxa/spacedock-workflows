# Decisions — reverse-recovery-audit-dangling-path

| when | stage | decision | authority | note |
| --- | --- | --- | --- | --- |
| 2026-07-19T03:10Z | shape (gate) | PROCEED — entity is now feed-eligible and WAITS for the L3 scheduler tick to dispatch it (hand-dispatch forbidden: this is the tick's live single-entity proof) | Captain 票ok (2026-07-19) + issue #69 AC explicitly delegated the fix-direction choice to shape; EM-drive grant covers the gate | Fix (b) chosen with primary-source evidence (mod header designates repo as source; no sync-manifest; sibling convention); guard spec fixture-backed RED/GREEN; out-of-scope discoveries (architecture-canon, canonical-doc-sync missing both tiers) flagged for follow-up todo at ship closeout, not bundled |
| 2026-07-19T11:17Z | shape (clarify) | Delegation token defined: a headless `/ship` run for this entity IS the tick's dispatch (not forbidden hand-dispatch) when a scheduler receipt younger than 30 minutes naming this slug exists under `.ship-flow-scheduler-receipts/` in the working repo root. Such a run must proceed through the full pipeline autonomously (EM-drive, park out-of-boundary questions) and stop at awaiting_merge. Hand-dispatch prohibition applies only to interactive human-initiated /ship runs WITHOUT such a receipt. | FO under captain EM-drive grant; root-cause fix for the 2026-07-19T11:15Z blocked run (spawned FO could not distinguish delegation) | First live tick spawn halted safely (fail-closed proven); this clause closes the delegation-ambiguity gap; adapter-level delegation token filed as follow-up todo |
| 2026-07-19T11:50Z | design (gate) | PROCEED — trivial-pass, additive-only, 0 open design questions; deltas Δ1 ship-shape:597 / Δ2 ship-plan:502 / Δ3 additive resolver in scripts/check-no-dangling.sh named; green-set enumerated (only reverse-recovery-audit reds pre-fix, nothing post-fix); grep of lib/__tests__ = 0 hits so Δ1/Δ2 break zero of 120 tests (AC-3 safe) | FO under captain EM-drive grant (delegation token: tick-dispatched autonomous drive) | Finding (c) carried into plan: qualifier vocab MUST cover agents-file "If the repo has … override" form (agents/science-officer-em.md:16-18), beyond shape literal list; plus resolver must be drivable against a scratch root for RED/GREEN fixtures |
| 2026-07-19T13:35Z | verify (gate) | PROCEED | Captain EM-drive grant (same condition set) | PASS with AC-1/2/3 independently verified incl. historical-bug reconstruction proof; 2 WARNING + 3 advisory deferred to TODO (non-blocking); cross_model declared false after 2 codex timeouts (circuit breaker) with visible Tier-B fallback — proportionality call documented, never silent |
