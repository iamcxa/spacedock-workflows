# C14 First Officer Dispatch Contract Implementation Plan

> **For executer:** REQUIRED SUB-SKILL: use `superpowers:executing-plans` task-by-task and preserve the Ship-Flow TDD ledger.

**Goal:** Ship two independent, mechanically checked transition contracts while retaining only Safe NARROW Cases 14–31.
**Architecture:** C14 recognizes FO entry by subject plus workflow edge; stage workers register completion through the existing helper on compatible folder entities. Legacy flat ledgers remain a no-helper path, and FO owns every next-stage entry and the clean execution range.
**Tech stack:** bash 3.2+, Git fixtures, Markdown stage skills, shellcheck.

<details>
<summary>Plan Imported Design DCs — generated by import-design-dcs.sh</summary>

## Plan Imported Design DCs

<!-- section:plan-imported-design-dcs -->
<!-- Generated by plugins/ship-flow/lib/import-design-dcs.sh from entity's ### Hand-off to Plan block. Do not hand-edit; rerun the script if the source hand-off changes. -->

### Imported design_constraints

| # | Type | Assertion | Rationale (D{N}) | Source artifact |
|---|---|---|---|---|
| 1 | contract | "FO stage entry is a subject-only dispatch/advance receipt bound to every entity after-stage and a declared direct or feedback workflow edge." | D1 | docs/ship-flow/c14-fo-dispatch-contract/design.md |
| 2 | contract | "On helper-compatible folder entities, stage workers register completion through advance-stage.sh against the completed stage and return next-stage entry to the FO; design receives the missing helper wiring." | D1 | docs/ship-flow/c14-fo-dispatch-contract/design.md |
| 3 | contract | "Legacy flat ledgers without the stage-artifact-links schema append stage reports without invoking advance-stage.sh and without mutating status or layout; the FO still owns the next entry." | D1 | docs/ship-flow/c14-fo-dispatch-contract/design.md |
| 4 | schema-contract | "The workflow README stages.states graph plus entity status remain canonical; no receipt field, database schema, migration, or authenticated provenance is added." | D1 | docs/ship-flow/c14-fo-dispatch-contract/design.md |
| 5 | contract | "Acceptance proves the real same-feature entry and a disposable next-ship sequence, while retaining manual-bypass detection and only Cases 14-31." | D2 | docs/ship-flow/c14-fo-dispatch-contract/design.md |
| 6 | contract | "Before review and ship, the FO prepares a clean merge range containing Cases 14-31 only; current-branch Cases 32-45 and unrelated harvest entities are excluded." | D2 | docs/ship-flow/c14-fo-dispatch-contract/design.md |

### Imported visible_surface_map

| # | ID | Type | Route | Selector Hint | Visible When | Coverage | Mapped By | Rationale (D{N}) | N/A Rationale |
|---|---|---|---|---|---|---|---|---|---|

### Imported render_fidelity_targets

| # | Selector | CSS Property | Expected Value | Rationale (D{N}) |
|---|---|---|---|---|

### Imported whole_page_visual_targets

| # | Route | Reference Artifact | Capture | Threshold | Rationale (D{N}) |
|---|---|---|---|---|---|

**Wave assignment**: each constraint is anchored to the task wave touching the affected contract; UI targets are empty for this non-UI entity.
<!-- /section:plan-imported-design-dcs -->

</details>

## Plan Output

### Research Summary

Producer verdict `GAPS`, addressed in this plan: live `origin/main..HEAD` already includes deferred Cases 32–45 and unrelated harvest work, so FO must create the clean execution base before any RED run. C14.1 maps to receipt/graph fixtures and C14 internals; C14.2 maps to parser/path fixtures and the workflow parser. Completion wiring is instruction-enforced for the legacy-flat fallback, so the honest proof is exact skill text plus helper rollback—not a stronger runtime router. Research second pass returned `APPROVED`; plan cross-review loop 2 returned `PROCEED` with `skill-coverage: PASS`.

<details>
<summary>Size re-evaluation</summary>

### Size Re-evaluation

- Sharp estimate: M (two ≤2-day children, ~2.2 days total).
- Research evidence: 14 implementation/test/doc surfaces, five serially bounded tasks.
- Adjusted size: M; scope remains two children plus their required completion-contract integration.

</details>

### Assumption Re-validation

- `check-invariants.sh:1455-1477,1637-1698`, Cases 14–31, and all stage helper call sites still match design claims.
- Real FO receipts `8b9488c` (`shape -> design`) and `7766dd7` (`design -> plan`) pass targeted C14 on this branch.
- Critical precondition: execute stops until FO provides a base where the new tests are absent/failing and Cases 32–45/unrelated harvest commits are excluded.

### Verification Spec

| DC | Verify Procedure | Expected |
|---|---|---|
| DC-1 | `bash plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh` | Cases 1–31 pass; canonical entry and feedback positives succeed while lookalikes fail. |
| DC-2 | `bash plugins/ship-flow/lib/__tests__/test-ship-unified-entry-routing.sh && bash plugins/ship-flow/lib/__tests__/test-stage-wiring.sh` | Exact entry/completion boundary and all after-stage/helper triples pass. |
| DC-3 | `bash plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh && ! rg -q '^echo "--- Case (3[2-9]|4[0-5]):' plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh` | Graph/parser checks stop at Case 31. |
| DC-4 | `CI=true bash plugins/ship-flow/bin/check-invariants.sh && for t in plugins/ship-flow/lib/__tests__/test-*.sh; do bash "$t" || exit; done && node --test plugins/ship-flow/bin/*.test.mjs && shellcheck plugins/ship-flow/bin/check-invariants.sh plugins/ship-flow/lib/advance-stage.sh plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh plugins/ship-flow/lib/__tests__/test-advance-stage.sh plugins/ship-flow/lib/__tests__/test-stage-wiring.sh plugins/ship-flow/lib/__tests__/test-ship-unified-entry-routing.sh && git diff --check` | Invariants, full shell, Node, shellcheck, and diff gates pass on the narrowed range. |

### Canonical Doc Actions

| Doc | Action | Source | Rationale |
|---|---|---|---|
| ROADMAP.md | update | plan | Add C14 to Now during active delivery; preserve deferred todos separately. |
| PRODUCT.md | skip | design | Internal pipeline correctness; no durable user-facing capability changes. |
| ARCHITECTURE.md | update | design | Record two sanctioned transition owners, graph-first validation, helper chain, and #38 provenance deferral. |

<details>
<summary>Task-to-scope anchoring</summary>

### Scope Anchoring

| Task | Shape/design mapping |
|---|---|
| T1 | C14.1 Cases 14–18; DC-1/DC-2; design constraints 1 and 5 |
| T2 | C14.1 Cases 19–25; DC-1/DC-3; design constraints 1 and 4 |
| T3 | C14.2 Cases 26–31; DC-3; design constraints 1, 4, and 6 |
| T4 | DC-2/DC-4; design constraints 2, 3, and 5 |
| T5 | DC-4; Canonical Doc Actions and clean-range audit |

</details>

### Plan

#### T1 — C14.1 FO receipt grammar (Cases 14–18)
task_id: T1
layer: L5
wave: W1
files: `plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh`, `plugins/ship-flow/bin/check-invariants.sh`, `plugins/ship-flow/lib/__tests__/test-ship-unified-entry-routing.sh`, `plugins/ship-flow/skills/ship/SKILL.md`
read_first: design.md Contracts 1–2; current files at `test-enforce-advance-stage.sh:1115-1144`, `check-invariants.sh:1455-1477`, `ship/SKILL.md:39-56`
skills_needed: [test, best-practices, test-driven-development]
reviewer_questions: [{lens: contract, question: "Are `dispatch: <summary> entering <stage>` and `advance: <summary> entering <stage>` subject-only, non-empty, and bound to every after-stage without weakening manual bypass?", affected_path_family: "plugins/ship-flow/bin/**, plugins/ship-flow/lib/**, plugins/ship-flow/skills/ship/**", evidence_required: "RED failure plus Cases 1-18 GREEN and literal grammar assertions"}]
tdd_contract:
  red_command: "bash plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh && bash plugins/ship-flow/lib/__tests__/test-ship-unified-entry-routing.sh"
  expected_red_failure: "Case 14 and the new exact fresh/reuse receipt assertions fail before the FO matcher/skill contract lands."
  green_command: "bash plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh && bash plugins/ship-flow/lib/__tests__/test-ship-unified-entry-routing.sh"
  refactor_check: "bash -n plugins/ship-flow/bin/check-invariants.sh plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh plugins/ship-flow/lib/__tests__/test-ship-unified-entry-routing.sh"
parallel_group: serial; depends_on: []; owned_paths: [plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh, plugins/ship-flow/bin/check-invariants.sh, plugins/ship-flow/lib/__tests__/test-ship-unified-entry-routing.sh, plugins/ship-flow/skills/ship/SKILL.md]; integration_owner: executer@c14-fo-dispatch-contract
steps: 1. Add Cases 14–18 and exact `/ship` strings only. 2. Run RED and retain failure output. 3. Add `_commit_has_fo_stage_entry_receipt` and minimal C14 call site plus skill text. 4. Run GREEN/refactor checks. 5. Commit explicit paths.
done: GREEN command passes with no commit allowlist or completion-signature forgery.
model: sonnet; canonical_doc_actions: shared table.

#### T2 — C14.1 graph-first legality (Cases 19–25)
task_id: T2
layer: L5
wave: W2
files: `plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh`, `plugins/ship-flow/bin/check-invariants.sh`
read_first: T1 output; `docs/ship-flow/README.md` root `stages.states`; historical Cases 19–25 only
skills_needed: [test, best-practices, test-driven-development]
reviewer_questions: [{lens: contract, question: "Does every FO/completion/exemption path cross declared direct-or-feedback graph validation first?", affected_path_family: "plugins/ship-flow/{bin,lib}/**", evidence_required: "RED failures for skipped/backward/mixed paths; Cases 1-25 GREEN"}]
tdd_contract:
  red_command: "bash plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh"
  expected_red_failure: "Cases 19, 20, 22, 23, and 25 expose skipped, backward, exemption, completion, and mixed-path bypasses."
  green_command: "bash plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh"
  refactor_check: "bash -n plugins/ship-flow/bin/check-invariants.sh plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh"
parallel_group: c14-w2; depends_on: [T1]; owned_paths: [plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh, plugins/ship-flow/bin/check-invariants.sh]; integration_owner: executer@c14-fo-dispatch-contract
steps: 1. Add Cases 19–25 only. 2. Run RED. 3. Add parent-revision workflow lookup and graph-first gate. 4. Run GREEN/refactor. 5. Commit explicit paths.
done: Cases 1–25 pass and every receipt/exemption route is graph-gated.
model: sonnet; canonical_doc_actions: shared table.

#### T3 — C14.2 bounded parser and path scope (Cases 26–31)
task_id: T3
layer: L5
wave: W3
files: `plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh`, `plugins/ship-flow/bin/check-invariants.sh`
read_first: T2 output; `check-invariants.sh:1334-1394,1494-1499`; stop before current Case 32
skills_needed: [test, best-practices, test-driven-development]
reviewer_questions: [{lens: parser, question: "Are root/bounds/indentation/normalization and single-word flat scope enforced without migration or merge logic?", affected_path_family: "plugins/ship-flow/{bin,lib}/**", evidence_required: "Cases 26/28-31 RED then 1-31 GREEN; Case 27 remains positive control"}]
tdd_contract:
  red_command: "bash plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh"
  expected_red_failure: "Cases 26 and 28-31 fail before root bounds, canonical indentation, and flat path scope are tightened; Case 27 stays green."
  green_command: "bash plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh"
  refactor_check: "bash -n plugins/ship-flow/bin/check-invariants.sh plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh"
parallel_group: serial; depends_on: [T2]; owned_paths: [plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh, plugins/ship-flow/bin/check-invariants.sh]; integration_owner: executer@c14-fo-dispatch-contract
steps: 1. Add Cases 26–31. 2. Run RED. 3. Tighten only root/bounds/indentation/normalization/path-scope hunks. 4. Run GREEN and assert no Cases 32–45. 5. Commit explicit paths.
done: DC-3 procedure passes and deferred history-shape behavior is absent.
model: sonnet; canonical_doc_actions: shared table.

#### T4 — Completion contract and immediate activation
task_id: T4
layer: L5
wave: W2
files: `plugins/ship-flow/lib/__tests__/test-stage-wiring.sh`, `plugins/ship-flow/lib/__tests__/test-advance-stage.sh`, `plugins/ship-flow/lib/advance-stage.sh`, and `SKILL.md` under each of `plugins/ship-flow/skills/ship-design`, `ship-plan`, `ship-execute`, `ship-verify`, `ship-review`, and `ship`
read_first: design.md Contract 2 and flat-ledger boundary; helper warning/rollback; current stage call sites
skills_needed: [test, test-driven-development, write-docs]
reviewer_questions: [{lens: contract, question: "Does a disposable repo prove FO entry, targeted C14, `design/design/design.md` completion, and separate FO plan entry while the literal flat-fallback instruction forbids helper invocation?", affected_path_family: "plugins/ship-flow/lib/**, plugins/ship-flow/skills/**", evidence_required: "focused RED/GREEN, rollback proof, exact triples, entry sequence, manual and cross-contract negatives"}]
tdd_contract:
  red_command: "bash plugins/ship-flow/lib/__tests__/test-advance-stage.sh && bash plugins/ship-flow/lib/__tests__/test-stage-wiring.sh"
  expected_red_failure: "New design/folder, flat-fallback, exact stage-triple, and separate FO-entry assertions fail before skill/helper wiring."
  green_command: "bash plugins/ship-flow/lib/__tests__/test-advance-stage.sh && bash plugins/ship-flow/lib/__tests__/test-stage-wiring.sh"
  refactor_check: "bash -n plugins/ship-flow/lib/advance-stage.sh plugins/ship-flow/lib/__tests__/test-advance-stage.sh plugins/ship-flow/lib/__tests__/test-stage-wiring.sh"
parallel_group: c14-w2; depends_on: [T1]; owned_paths: [plugins/ship-flow/lib/__tests__/test-stage-wiring.sh, plugins/ship-flow/lib/__tests__/test-advance-stage.sh, plugins/ship-flow/lib/advance-stage.sh, plugins/ship-flow/skills/ship-design/SKILL.md, plugins/ship-flow/skills/ship-plan/SKILL.md, plugins/ship-flow/skills/ship-execute/SKILL.md, plugins/ship-flow/skills/ship-verify/SKILL.md, plugins/ship-flow/skills/ship-review/SKILL.md, plugins/ship-flow/skills/ship/SKILL.md]; integration_owner: executer@c14-fo-dispatch-contract
steps: 1. Build a disposable repo with `origin/main`, workflow shape/design/plan, and a helper-compatible folder entity. 2. Add assertions for `dispatch: example entering design` → targeted C14, `design/design/design.md` idempotent completion, then `advance: example entering plan` → targeted C14; add arbitrary-manual and completion-signature-skipped-edge negatives. 3. Add flat exit-10 rollback plus a literal ship-design assertion: `Legacy flat entities without stage-artifact-links MUST NOT invoke advance-stage.sh; append the report without status or layout mutation and return to FO.` 4. Run RED. 5. Add helper usage/design fallback and exact triples `plan/plan/plan.md`, `execute/execute/execute.md`, `verify/verify/verify.md`, `ship/review/review.md`, `ship/ship/ship.md`; run GREEN/refactor and commit explicit paths.
done: disposable next-ship sequence and both negatives pass; flat rollback proves safe refusal while the literal skill assertion—not runtime code—pins routing.
model: sonnet; canonical_doc_actions: shared table.

#### T5 — Canonical contract and range audit
task_id: T5
layer: meta
wave: W4
files: `plugins/ship-flow/INVARIANTS.md`, `ARCHITECTURE.md`, `ROADMAP.md`
read_first: completed T1–T4 diffs; design Canonical/Schema boundary; Canonical Doc Actions
skills_needed: [write-docs]
reviewer_questions: [{lens: architecture, question: "Do canon and the final range describe exactly two owners and exclude deferred migration/merge/provenance semantics?", affected_path_family: "{ARCHITECTURE.md,ROADMAP.md,plugins/ship-flow/INVARIANTS.md}", evidence_required: "patch-map diff, targeted C14, origin/main range audit"}]
TDD: skip -- docs-only canonical synchronization; existing invariant and diff gates are the alternate validation.
parallel_group: serial; depends_on: [T3, T4]; owned_paths: [plugins/ship-flow/INVARIANTS.md, ARCHITECTURE.md, ROADMAP.md]; integration_owner: executer@c14-fo-dispatch-contract
steps: 1. Patch section-tagged root docs via `patch-map.sh`. 2. Correct stale completion-signature wording and add the architecture decision. 3. Add C14 to Roadmap Now. 4. Audit `origin/main..HEAD` for Cases 32–45/unrelated harvest. 5. Run full DC-4 gates and commit explicit paths.
range_assertion: `bash -c 'set -e; allowed="^(ARCHITECTURE.md|ROADMAP.md|docs/ship-flow/c14-fo-dispatch-contract.md|docs/ship-flow/c14-fo-dispatch-contract/(shape.md|design.md|plan.md|tdd-ledger.txt|tdd-ledger.jsonl)|plugins/ship-flow/INVARIANTS.md|plugins/ship-flow/bin/check-invariants.sh|plugins/ship-flow/lib/advance-stage.sh|plugins/ship-flow/lib/__tests__/(test-enforce-advance-stage.sh|test-advance-stage.sh|test-stage-wiring.sh|test-ship-unified-entry-routing.sh)|plugins/ship-flow/skills/(ship|ship-design|ship-plan|ship-execute|ship-verify|ship-review)/SKILL.md)$"; test -z "$(git diff --name-only origin/main...HEAD | rg -v "$allowed" || true)"; ! rg -q "Case (3[2-9]|4[0-5]):|_entity_(layout_identity|migration_paths_match)|setup_retirement_or_migration_fixture|commit_parents|rename_before_paths" plugins/ship-flow/bin/check-invariants.sh plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh'`
done: run `range_assertion`, targeted C14, DC-4, and `git diff --check`; all exit 0 on the FO-narrowed range.
model: sonnet; canonical_doc_actions: shared table.

<details>
<summary>Context routing manifest and receipt</summary>

<!-- section:context-routing-manifest -->
```yaml
context-routing-manifest:
  domain_matches: [schema]
  knowledge_modules: [plugins/ship-flow/references/domain-knowledge/schema.md]
  required_skills: []
  stage_hints: {plan: []}
  consumer_obligations: [preserve workflow graph and status schema, add no receipt field, route contract questions through D1 and D2]
  future_provider_boundary: "Optional provider hints are non-authoritative; local registry remains canonical."
```
<!-- /section:context-routing-manifest -->

## Context Routing Receipt

| Manifest row | Plan task skill mapping | Reviewer questions | domain_acceptance_checklist row |
|---|---|---|---|
| schema/knowledge | T1–T4 use contract/parser/test skills; no project-db skill | T1–T4 schema/contract questions | T1–T4 rows below |
| provider boundary | none; explicit skip because no provider is configured | T5 range/canon question | T5 row below |

</details>

## Context Manifest

- **Skills loaded**: ship-plan, superpowers:writing-plans, ship-flow:test-driven-development, ship-runtime-detect.
- **INVARIANTS sections read**: Principles 6, 8, and 15 (`plugins/ship-flow/INVARIANTS.md:223-344,561-590`).
- **Architecture docs consulted**: PRODUCT.md, ROADMAP.md, ARCHITECTURE.md, design.md, schema knowledge module.
- **Domains touched**: schema contract only; no database layer.
- **Lens dispatched**: none (no tag/customer/event-saga trigger match).
- **Lens findings integrated**: 0 integrated, 0 deferred, 0 ignored.
- **Folder guidance**: files=`plugins/ship-flow/{bin,lib,skills}/**`, root docs → folder_guidance_files=[]; folder_guidance_skills=[]; codex_context_boundary=root AGENTS.md/CLAUDE.md intentionally excluded from folder_guidance_files.

## Plan Report

status: passed
stage_cost: local planning + 3 read-only research/review dispatches
iterations: 1 self-review + 2 plan-cross-review loops
dimensions: requirement, completeness, dependencies, placeholders, signatures, minimality, TDD, line anchors, design import, context, skills all pass
reviewer_verdict: REVISE_FIXED
scope_anchoring: 5/5 tasks mapped; Cases 32–45 and unrelated harvest excluded
task_count: 5
model_split: 5 sonnet implementation tasks
skill-coverage: PASS

### Metrics

status: passed
duration_minutes: 28
iteration_count: 3
task_count: 5
verification_spec_count: 4
model_split: 5 sonnet

### Hand-off to Execute

<!-- section:hand-off-to-execute -->
- **execution_precondition**: FO must provide a clean execution/merge range containing plan artifacts and Safe NARROW Cases 14–31 only; if focused RED commands pass before production edits, stop and return to FO.
- **clean_range_strategy**: create a new FO-owned branch/worktree from `origin/main`, carry the C14 entity artifacts forward, and reconstruct the slices below through T1–T4. Preserve this broad branch as evidence; do not rewrite it or cherry-pick the source commits wholesale.
- **clean_ref_receipt**: FO records `CLEAN_REF=<new branch head>` and must pass `test "$(git merge-base origin/main "$CLEAN_REF")" = "$(git rev-parse origin/main)" && test -z "$(git diff --name-only origin/main..."$CLEAN_REF" -- plugins/ship-flow/bin/check-invariants.sh plugins/ship-flow/lib/advance-stage.sh plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh plugins/ship-flow/lib/__tests__/test-stage-wiring.sh plugins/ship-flow/lib/__tests__/test-advance-stage.sh plugins/ship-flow/lib/__tests__/test-ship-unified-entry-routing.sh plugins/ship-flow/skills/ship/SKILL.md plugins/ship-flow/skills/ship-design/SKILL.md plugins/ship-flow/skills/ship-plan/SKILL.md plugins/ship-flow/skills/ship-execute/SKILL.md plugins/ship-flow/skills/ship-verify/SKILL.md plugins/ship-flow/skills/ship-review/SKILL.md)" && ! git diff --name-only origin/main..."$CLEAN_REF" | rg 'fixture-pollution|root-readme|shape-confirm|plugin-readme'` before execute dispatch.
- **red_stop_rule**: after each task adds tests but before production edits, run its RED command; if it exits 0, stop and return to FO because the clean ref still contains already-green implementation or the test does not prove missing behavior.
- **tdd-ledger**: `docs/ship-flow/c14-fo-dispatch-contract/tdd-ledger.jsonl`; validate with `python3 plugins/ship-flow/lib/validate-tdd-ledger.py --plan docs/ship-flow/c14-fo-dispatch-contract/plan.md --require-ledger-jsonl docs/ship-flow/c14-fo-dispatch-contract/tdd-ledger.jsonl`.
- **wave_order**: W1 T1 → W2 (T2 ∥ T4) → W3 T3 → W4 T5.
- **critical_assumptions**: FO range is narrow before RED; workflow README remains graph authority; flat fallback remains instruction-enforced only.
- **architecture_context**: update INVARIANTS/ARCHITECTURE/ROADMAP; preserve PRODUCT/workflow/entity schema.
- **stub_flags**: none.
- **skills_needed_summary**: T1–T3 test/best-practices/TDD; T4 test/TDD/write-docs; T5 write-docs; three distinct lists.

<details>
<summary>Machine-readable Safe NARROW source-slice manifest</summary>

```yaml
clean_range_manifest:
  base: origin/main
  strategy: new-worktree-reconstruct-with-red-green
  preserve_current_branch: true
  slices:
    - source_commit: f8fc638
      mode: reconstruct
      include: [Cases 14-18, _commit_has_fo_stage_entry_receipt, FO call site, ship receipt grammar]
      exclude: [flat entity report]
    - source_commit: 347cfe2
      mode: reconstruct
      include: [Cases 19-22, workflow direct-feedback lookup, graph gate]
      exclude: [flat entity report]
    - source_commit: 2f4afbe
      mode: reconstruct
      include: [Cases 23-25, graph-before-every-receipt path, normalization]
      exclude: [flat entity report]
    - source_commit: be5d071
      mode: reconstruct
      include: [Cases 26-28, root sibling bound, single-word flat path scope]
      exclude: [flat entity report]
    - source_commit: 4b1b35b
      mode: reconstruct
      include: [Case 29, root stages key requirement]
      exclude: [flat entity report]
    - source_commit: cd957c3
      mode: hunk-only
      include_code_anchors: ["/^    - name:/", "/^      feedback-to:/"]
      include_test_anchors: [nested-stage-name, nested-feedback, Case 30, Case 31]
      exclude: [_entity_layout_identity, migration arrays and pairing, setup_retirement_or_migration_fixture, Cases 32-34]
  hard_exclude: [commits after cd957c3 implementing Cases 35-45, unrelated harvest entities]
```

</details>

| Task ID | Parallel Group | Depends On | Owned Paths | Integration Owner |
|---|---|---|---|---|
| T1 | serial | — | `plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh`; `plugins/ship-flow/bin/check-invariants.sh`; `plugins/ship-flow/lib/__tests__/test-ship-unified-entry-routing.sh`; `plugins/ship-flow/skills/ship/SKILL.md` | executer@c14-fo-dispatch-contract |
| T2 | c14-w2 | T1 | `plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh`; `plugins/ship-flow/bin/check-invariants.sh` | executer@c14-fo-dispatch-contract |
| T3 | serial | T2 | `plugins/ship-flow/lib/__tests__/test-enforce-advance-stage.sh`; `plugins/ship-flow/bin/check-invariants.sh` | executer@c14-fo-dispatch-contract |
| T4 | c14-w2 | T1 | `plugins/ship-flow/lib/__tests__/test-stage-wiring.sh`; `plugins/ship-flow/lib/__tests__/test-advance-stage.sh`; `plugins/ship-flow/lib/advance-stage.sh`; `plugins/ship-flow/skills/ship-design/SKILL.md`; `plugins/ship-flow/skills/ship-plan/SKILL.md`; `plugins/ship-flow/skills/ship-execute/SKILL.md`; `plugins/ship-flow/skills/ship-verify/SKILL.md`; `plugins/ship-flow/skills/ship-review/SKILL.md`; `plugins/ship-flow/skills/ship/SKILL.md` | executer@c14-fo-dispatch-contract |
| T5 | serial | T3,T4 | `plugins/ship-flow/INVARIANTS.md`; `ARCHITECTURE.md`; `ROADMAP.md` | executer@c14-fo-dispatch-contract |

| Task ID | Verify Lens | Reviewer Question | Affected Path Family | Required Skills | Evidence Required |
|---|---|---|---|---|---|
| T1 | contract | Exact subject/after-stage binding with manual bypass intact? | bin/lib/skills | test,best-practices,TDD | Cases 1–18 + exact strings |
| T2 | contract | Graph-first for every route? | bin/lib | test,best-practices,TDD | Cases 1–25 |
| T3 | parser | Root/bounds/indentation/path scope only? | bin/lib | test,best-practices,TDD | Cases 1–31; no 32–45 |
| T4 | contract | Folder completion separate; flat fallback honest? | lib/skills | test,TDD,write-docs | focused wiring/rollback tests |
| T5 | architecture | Canon and range match Safe NARROW? | canonical docs | write-docs | diff + merge-range audit |

| Doc | Action | Source | Rationale |
|---|---|---|---|
| ROADMAP.md | update | plan | Active C14 row. |
| PRODUCT.md | skip | design | Internal correctness only. |
| ARCHITECTURE.md | update | design | Two-owner decision. |

- **folder_guidance**: T1–T5 have no non-root guidance files; preserve the Context Manifest boundary.
<!-- /section:hand-off-to-execute -->
