# roborev-execute-terminalization — Plan (FO state-op, folded)
T1: main-based worktree `fo/roborev-execute-terminalization`; `git mv` entity → docs/ship-flow/_archive/; frontmatter: status done, verdict REJECTED, completed+archived stamps, worktree cleared, no pr field; body postscript (parked rationale, #36/#37 pointers, local branch d175178 preserved by SHA).
T2: push branch; PR to main (refs #88, unblocks #80); arm auto-merge (captain decision #4 flow); bounded CI watch.
T3: after merge — `gh pr update-branch 80` → fresh invariants run → AC-1 evidence; mirror archive on muscat-v1.
Runtime: no tests move (state-only); the archive PR's full-corpus invariants run IS the verification.
