<!-- section:plan-report -->
# Plan Attempt Vertical Implementation Plan

> **For executer:** REQUIRED SUB-SKILL: use `superpowers:executing-plans` and `ship-flow:test-driven-development`; preserve RED before GREEN and commit only explicit owned paths.

**Goal:** Make the real First Officer `plan` caller consume one fresh `stage-attempt-v1` from creation through one authoritative passed return, one terminal event, and one completion checkpoint.
**Architecture:** Extend the existing completion lifecycle with plan-only attempt begin/checkpoint wrappers, route only the `plan` row in `ship/SKILL.md` through them, and complete the attempt helper’s missing happy-path terminal action. The caller wraps the worker’s unchanged `completion-v1` receipt in caller-owned attempt metadata, accepts it, checkpoints completion, and contributes one terminal record; crash/replay recovery, execute adoption, and routing remain later children.
**Tech stack:** Bash 3.2+, Git common-dir fixtures, Markdown caller contract, ShellCheck, Node 18+ repository gates.

## Research Summary

- `plugins/ship-flow/skills/ship/SKILL.md:125-136` is the real FO caller: begin completion lease, prepend authority, dispatch, checkpoint the unchanged worker return, then make any separate Contract-1 entry. `ship-plan/SKILL.md` is the worker and is not an owned caller surface.
- `fo-stage-attempt.sh` protocol/clock authority is **WORKING_UNIT_UNPROVEN** at the caller seam: 006.1 verified exact matrices at source lineage `548b338`, but no production caller invokes it. `accept-return` persists the exact bundle, yet the command allowlist has no terminal action (`fo-stage-attempt.sh:612-652`). Happy-path `returned -> terminal` is therefore **EXISTS_BROKEN**; crash/replay recovery is a separate child. Disproof hook: one real caller run that produces one canonical terminal record after one accepted return.
- `fo-completion-lifecycle.sh` is **WORKING** for completion-only dispatch/checkpoint, and `ship/SKILL.md` is **EXISTS_BROKEN** for plan-attempt consumption because it sends raw `completion-v1` through the generic path. The focused `test-stage-wiring.sh --plan-attempt` case is **MISSING**: two searches (`rg 'plan-attempt|stage-attempt'` in that file and inspection of all current selectors) find no equivalent. Disproof hook: an existing real-helper selector proving exact plan counts.
- Research producer found prior fake-green lifecycle fixtures drifted from the production reconciler; the plan therefore extends `test-stage-wiring.sh` and uses real `fo-stage-attempt.sh`, `fo-completion-lifecycle.sh`, `advance-stage.sh`, and reconciliation. Only the dispatch boundary is counted by the fixture.
- A passed folder bundle must carry exactly one completion frame bound to the same ref/before/entity/stage/artifact before checkpoint (`fo-stage-attempt.sh:562-581`). `returned` is not terminal: this child adds only the uninterrupted happy-path terminal record and cleanup after checkpoint; crash windows, provisional adoption, replay/already-recorded behavior, dirty-state preservation, and interrupted returns remain recovery scope.
- Runtime is Tier 2: Bash 3.2+/Node 18+ (`ARCHITECTURE.md:124`); local ShellCheck and Node are available. No gate/skill-routing config or non-root guidance applies. The final full suite runs once after the focused changed-surface checks.

Reviewer assessment: GAPS_ADDRESSED — corrected the returned-versus-terminal distinction and named the FO caller as the outer-bundle producer; completion worker bytes remain unchanged.

## Size Re-evaluation

- Sharp estimate: S.
- Research evidence: four modified implementation/test/caller files plus the plan artifact place the slice in the mechanical 4-10-file band.
- Adjusted size: M, delivered as one serial task because all four files share one authority seam.
- Token budget: unchanged; bounded child cap and 60m/90m/120m dispatch contract govern execution.

Any need to edit `completion-v1.sh`, schema, worker skills, execute, recovery tests, scheduler, dispatcher, or siblings returns **NARROW** rather than expanding.

## Plan Imported Design DCs

<!-- section:plan-imported-design-dcs -->
<!-- Generated by plugins/ship-flow/lib/import-design-dcs.sh from entity's ### Hand-off to Plan block. Do not hand-edit; rerun the script if the source hand-off changes. -->

**Status**: design-skipped (entity has `affects_ui: false`; design ran a Phase 0 trivial-pass — not bypassed; the design stage always runs, INVARIANTS Principle 11).

No design DCs to import. Plan proceeds without UI/render-fidelity DC imports.

<!-- /section:plan-imported-design-dcs -->

## Verification Spec

| DC | Verify Procedure | Expected |
|---|---|---|
| DC-1 real caller | `bash plugins/ship-flow/lib/__tests__/test-stage-wiring.sh --plan-attempt` | Exactly one real attempt begin, fixture-observed worker dispatch, authoritative `accept-return`, completion checkpoint, terminal event/duration contribution, and plan artifact registration; success leaves no private open/returned WAL or sidecar. |
| DC-2 typed authority | `bash plugins/ship-flow/lib/__tests__/test-stage-wiring.sh --plan-attempt && bash plugins/ship-flow/lib/__tests__/test-stage-attempt-v1-contract.sh && STAGE_ATTEMPT_CLOCK_CASE=nonterminal bash plugins/ship-flow/lib/__tests__/test-stage-attempt-clock.sh` | Fresh mode is explicit and differs from resume; attempt ID, 1200-second budget, lease, ref/before OIDs, worker, artifact, outcome, and terminal event remain bound by the shipped helper. |
| DC-3 scoped verification | `bash plugins/ship-flow/lib/__tests__/test-stage-wiring.sh --completion-lifecycle && bash plugins/ship-flow/lib/__tests__/test-stage-wiring.sh --completion-lifecycle-faults && bash -n plugins/ship-flow/lib/fo-completion-lifecycle.sh plugins/ship-flow/lib/__tests__/test-stage-wiring.sh && shellcheck plugins/ship-flow/lib/fo-completion-lifecycle.sh plugins/ship-flow/lib/__tests__/test-stage-wiring.sh && git diff --check` | Existing completion/fault behavior and changed shell surfaces pass before the full suite. |
| Final integrated HEAD | `bash -c 'rc=0; for t in plugins/ship-flow/lib/__tests__/test-*.sh; do bash "$t" || rc=1; done; CI=true bash plugins/ship-flow/bin/check-invariants.sh || rc=1; node --test plugins/ship-flow/bin/*.test.mjs || rc=1; bash scripts/check-version-triple.sh || rc=1; bash scripts/check-no-dangling.sh || rc=1; exit "$rc"'` | One reserved all-gates run completes. Owned surfaces must pass; recovery/route/#21 or other unrelated failures are recorded with exact command/output/owner and deferred, never repaired here. |

### Canonical Doc Actions

| Doc | Action | Source | Rationale |
|---|---|---|---|
| `ROADMAP.md` | skip | plan | The Phase-A reshape receipt owns child ordering; this internal vertical does not change roadmap placement. |
| `PRODUCT.md` | skip | spec | Caller correctness changes no external capability or product promise. |
| `ARCHITECTURE.md` | update | plan | At review, record that the real plan caller consumes the shipped typed attempt protocol while completion-v1 and Contract-1 separation remain unchanged. |

## Plan

### T1 — Consume one fresh plan attempt in the real FO caller

task_id: T1
layer: L5
wave: W1
files: modify `plugins/ship-flow/lib/__tests__/test-stage-wiring.sh`, `plugins/ship-flow/lib/fo-stage-attempt.sh`, `plugins/ship-flow/lib/fo-completion-lifecycle.sh`, `plugins/ship-flow/skills/ship/SKILL.md`; read-only regressions `plugins/ship-flow/lib/completion-v1.sh`, `plugins/ship-flow/lib/advance-stage.sh`
read_first: shape acceptance; Phase-A receipt lines 39-50; `ship/SKILL.md:113-136`; lifecycle helper; attempt helper begin/return paths; current stage-wiring completion selectors
classification: attempt protocol/clock WORKING_UNIT_UNPROVEN at the caller seam, happy-path terminal action EXISTS_BROKEN because the shipped command allowlist stops at returned, completion lifecycle WORKING, plan caller EXISTS_BROKEN at the missing attempt seam, focused selector MISSING by the two-strategy search in Research Summary; no new file is justified.
skills_needed: [test, best-practices, test-driven-development]
reviewer_questions: [{lens: contract, question: "Does only the real plan caller create one explicit fresh attempt, wrap the unchanged worker receipt in caller-owned authority, accept it, checkpoint once, contribute one distinct terminal event/duration, and keep next-stage entry separate?", affected_path_family: "plugins/ship-flow/{lib,skills/ship}/**", evidence_required: "focused RED/GREEN with exact 1/1/1 counters, real terminal record and cleanup, frozen completion bytes, protocol/clock compatibility, one final full-suite classification"}]
tdd_contract:
  red_command: "bash plugins/ship-flow/lib/__tests__/test-stage-wiring.sh --plan-attempt"
  expected_red_failure: "After adding only the focused selector/assertions, it fails because production has no plan-attempt begin/checkpoint or terminal command and ship/SKILL routes plan through raw completion-v1; missing fixture setup, a returned-only WAL, or mocked helper success is invalid RED."
  green_command: "bash plugins/ship-flow/lib/__tests__/test-stage-wiring.sh --plan-attempt && bash plugins/ship-flow/lib/__tests__/test-stage-wiring.sh --completion-lifecycle && bash plugins/ship-flow/lib/__tests__/test-stage-wiring.sh --completion-lifecycle-faults && bash plugins/ship-flow/lib/__tests__/test-stage-attempt-v1-contract.sh && STAGE_ATTEMPT_CLOCK_CASE=nonterminal bash plugins/ship-flow/lib/__tests__/test-stage-attempt-clock.sh"
  refactor_check: "bash -n plugins/ship-flow/lib/fo-stage-attempt.sh plugins/ship-flow/lib/fo-completion-lifecycle.sh plugins/ship-flow/lib/__tests__/test-stage-wiring.sh && shellcheck plugins/ship-flow/lib/fo-stage-attempt.sh plugins/ship-flow/lib/fo-completion-lifecycle.sh plugins/ship-flow/lib/__tests__/test-stage-wiring.sh && git diff --exit-code 548b3388400dd70651351b24f4e0ef537f2f4c38 -- plugins/ship-flow/lib/completion-v1.sh && git diff --check"
parallel_group: serial
depends_on: []
owned_paths: [plugins/ship-flow/lib/__tests__/test-stage-wiring.sh, plugins/ship-flow/lib/fo-stage-attempt.sh, plugins/ship-flow/lib/fo-completion-lifecycle.sh, plugins/ship-flow/skills/ship/SKILL.md]
integration_owner: executer@006-plan-attempt-vertical
canonical_doc_actions: shared table
steps: 1. Add `--plan-attempt` to the existing disposable-repo harness. Count exactly one dispatch, authoritative accept, and distinct terminal contribution while exercising real begin/accept/checkpoint/terminal helpers; assert one terminal-history line, one tracked exact return sidecar, one duration, cleaned private WAL/sidecar, unchanged `status: plan`, and one `stage_outputs.plan`. Run RED before production edits. 2. Add only the uninterrupted `terminal` transition to `fo-stage-attempt.sh`: consume the exact accepted sidecar, run the supplied completion checkpoint for passed folders, atomically contribute one canonical terminal record plus tracked return sidecar, reconcile those paths, and clean private state after success. Do not implement crash failpoints, provisional adoption, replay/already-recorded, interrupted terminalization, dirty-state recovery, route, or continuation policy. 3. Add plan-only lifecycle begin/checkpoint functions. Acquire the existing completion lease, invoke attempt `begin` with ordinal 0/fresh count 0 and caller timestamp, expose exact fresh assignment fields, then after dispatch construct the outer bundle from caller-owned begin/WAL authority plus the worker's unchanged completion receipt, accept it, invoke terminal with the existing checkpoint, and require one terminal disposition. Fail closed and retain evidence on uncertainty. 4. Change only the `plan` branch in `ship/SKILL.md`; leave all other stages on raw completion and keep Contract-1 entry separate. Run focused GREEN/changed-surface checks, review the four-path diff, then run the reserved all-gates command once and classify unowned failures without repair.
review_gate: PROCEED only with exact 1/1/1 dispatch/authoritative-return/terminal-contribution evidence, fresh-vs-resume distinction, typed authority across the same seam, unchanged completion bytes, protocol/clock regressions GREEN, and no crash/replay/execute/generalization/scheduler/dispatcher/#21/XFAIL/sibling/unrelated repair; otherwise NARROW.
model: sonnet

<!-- section:context-routing-manifest -->
```yaml
context-routing-manifest:
  domain_matches: []
  knowledge_modules: []
  required_skills: []
  stage_hints: {plan: []}
  consumer_obligations: [preserve typed FO authority, keep completion-v1 frozen, keep Contract-1 entry separate, stop before recovery or execute]
  future_provider_boundary: "Optional provider hints are non-authoritative; no provider is configured for this workflow-local seam."
```
<!-- /section:context-routing-manifest -->

## Context Routing Receipt

| Manifest row | Plan task skill mapping | Reviewer questions | domain_acceptance_checklist row |
|---|---|---|---|
| empty domain/knowledge/required-skills/stage-hints | T1 uses repository test/TDD skills, not a domain pack | T1 exact FO contract question | T1 row below |
| consumer obligations/provider boundary | T1 owns every obligation; no provider mapping | T1 scope and authority question | T1 row below |

## Context Manifest

- **Skills loaded:** spacedock:ensign, ship-flow:ship-plan, superpowers:writing-plans, ship-flow:test-driven-development, ship-flow:ship-runtime-detect, reverse-recovery-audit.
- **INVARIANTS sections read:** Principle 15 two transition owners and same-run checkpoint (`plugins/ship-flow/INVARIANTS.md:597-625`); plan schema/TDD/context requirements in `ship-plan/SKILL.md`.
- **Architecture docs consulted:** `PRODUCT.md`, `ROADMAP.md`, `ARCHITECTURE.md`, shape/design, Phase-A reshape receipt, 006.1 verify/ship evidence, superseded umbrella design/plan only for seam recovery.
- **Domains touched:** workflow-local FO plan caller and completion lifecycle; no schema, application, scheduler, dispatcher, or event-saga domain.
- **Lens dispatched:** none; architecture-lens registry has no trigger for this workflow-local shell seam.
- **Lens findings integrated:** 0 integrated, 0 deferred, 0 ignored.
- **Folder guidance:** files=`plugins/ship-flow/lib/__tests__/test-stage-wiring.sh`, `plugins/ship-flow/lib/fo-stage-attempt.sh`, `plugins/ship-flow/lib/fo-completion-lifecycle.sh`, `plugins/ship-flow/skills/ship/SKILL.md`, `docs/ship-flow/006-plan-attempt-vertical/**` -> folder_guidance_files=[]; folder_guidance_skills=[]; codex_context_boundary=root AGENTS.md/CLAUDE.md intentionally excluded from folder_guidance_files.

## Plan Report

status: passed
stage_cost: $0.00 (3 read-only dispatches: research producer, research reviewer, plan cross-review)
iterations: 4 (two author/research reviews plus two cross-review repair rounds)
dimensions: requirement coverage, exact files, dependency safety, minimality, TDD, reverse recovery, verification ordering, scope boundary, context manifest, and skill coverage
reviewer_verdict: REVISE_FIXED — size/routing and durable-ledger handoff findings repaired; final exact-HEAD re-review required before completion
scope_anchoring: 1/1 task maps to the plan-caller vertical; all later-child and unrelated surfaces excluded
task_count: 1
model_split: 1 sonnet
skill-coverage: PASS

### Metrics

status: passed
duration_minutes: 48
iteration_count: 4
task_count: 1
verification_spec_count: 4
model_split: 1 sonnet

### Hand-off to Execute

<!-- section:hand-off-to-execute -->
- **tdd-ledger:** `docs/ship-flow/006-plan-attempt-vertical/tdd-ledger.jsonl`; `python3 plugins/ship-flow/lib/validate-tdd-ledger.py --plan docs/ship-flow/006-plan-attempt-vertical/plan.md` -> `status=pass records=1`; `python3 plugins/ship-flow/lib/validate-tdd-ledger.py --plan docs/ship-flow/006-plan-attempt-vertical/plan.md --require-ledger-jsonl docs/ship-flow/006-plan-attempt-vertical/tdd-ledger.jsonl` -> `status=pass records=1` persisted-ledger proof.
- **wave_order:** W1 T1 -> explicit-path review/commit -> one reserved final integrated suite.
- **critical_assumptions:** source lineage `548b338` remains reachable; protocol/clock behavior stays compatible and `completion-v1.sh` stays byte-identical; the entity remains at `status: plan`; Bash 3.2+/Node 18+ exist.
- **architecture_context:** review updates `ARCHITECTURE.md`; `PRODUCT.md` and `ROADMAP.md` skip with recorded rationale.
- **stub_flags:** none.
- **skills_needed_summary:** T1 uses `[test, best-practices, test-driven-development]`; no domain pack, framework lens, or folder-guidance skill applies.

#### Plan Parallelization Manifest

| Task ID | Parallel Group | Depends On | Owned Paths | Integration Owner |
|---|---|---|---|---|
| T1 | serial | none | `test-stage-wiring.sh`, `fo-stage-attempt.sh`, `fo-completion-lifecycle.sh`, `ship/SKILL.md` | executer@006-plan-attempt-vertical |

#### Domain Acceptance Checklist

| Task ID | Verify Lens | Reviewer Question | Affected Path Family | Required Skills | Evidence Required |
|---|---|---|---|---|---|
| T1 | exact FO attempt contract | Does plan alone traverse one fresh typed attempt and preserve independent completion/entry authority? | `plugins/ship-flow/{lib,skills/ship}/**` | test,best-practices,TDD | exact 1/1/1 counters, real returned bytes/state, focused compatibility, frozen helper diff, final suite receipt |

#### Canonical Doc Actions Summary

| Doc | Action | Source | Rationale |
|---|---|---|---|
| `ROADMAP.md` | skip | plan | Phase-A receipt owns ordering. |
| `PRODUCT.md` | skip | spec | No external capability change. |
| `ARCHITECTURE.md` | update | plan | Record verified real-caller attempt consumption at review. |

- **narrow_boundary:** no crash/replay recovery, execute adoption/generalization, scheduler, dispatcher, #21, XFAIL/future-RED registry, sibling work, or unrelated repair. An unrelated final-suite failure is evidence to record and defer, not new ownership.
<!-- /section:hand-off-to-execute -->

<!-- /section:plan-report -->
